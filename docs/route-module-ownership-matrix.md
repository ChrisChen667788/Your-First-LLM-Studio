# Route and Module Ownership Matrix

Last updated: 2026-08-21
Refactor phase: Phase 5 release-evidence checkpoint, tracked against the Phase 0 map

This matrix captures the current route ownership, the intended product surface, and the module boundary that should own future implementation. It is the routing companion to [`architecture-refactor-plan.md`](./architecture-refactor-plan.md).

## Ownership Rules

- Product routes are for daily user workflows.
- Admin routes are for monitoring, configuration, management, audit, and worker operations.
- API routes should be thin transport adapters over feature application services.
- UI components should depend on feature contracts and application ports, not deep persistence or provider internals.
- Cross-feature handoff should happen through typed contracts, timeline events, or report/artifact references.

## Current Route Matrix

| Route | Current Role | Current Primary Modules | Future Product Surface | Future Owner | Migration Notes |
| --- | --- | --- | --- | --- | --- |
| `/` | Landing and product introduction. | `app/page.tsx`, layout/i18n copy. | `/` | `features/marketing` or app shell | Keep lightweight and release-oriented. |
| `/agent` | Main Agent workbench, chat, target selection, embedded Compare entry, runtime summary. | `components/agent/AgentWorkbench.tsx`, `features/agent/chat-actions.ts`, `features/agent/turn-lifecycle.ts`, `features/agent/secondary-analysis-panel.tsx`, `features/agent/runtime-view-model.ts`, `features/agent/session-model.ts`, `features/agent/session-persistence.ts`, `features/agent/session-apply.ts`, `features/agent/session-hydration.ts`, `features/agent/session-server-sync.ts`, `features/agent/session-sidebar-selectors.ts`, `features/agent/workbench-preferences.ts`, `features/agent/workbench-shell-state.ts`, `features/agent/runtime-shell-state.ts`, `features/agent/runtime-connection-shell.ts`, `features/agent/runtime-connection-composition.ts`, `features/agent/runtime-actions.ts`, `features/agent/runtime-formatters.ts`, `features/agent/runtime-status-rail.tsx`, `features/agent/runtime-rail-composition.ts`, `features/agent/target-catalog-panel.tsx`, `features/agent/target-profile-panel.tsx`, `features/agent/workbench-sidebar.tsx`, `features/agent/sidebar-composition.ts`, `features/agent/tool-registry-panel.tsx`, `features/agent/agent-composer-form.tsx`, `features/agent/session-tools-panel.tsx`, `features/agent/workspace-file-preview-panel.tsx`, `features/agent/workspace-file-actions.ts`, `features/agent/agent-tool-review-panel.tsx`, `features/agent/agent-tool-run-review-card.tsx`, `features/agent/agent-transcript-panel.tsx`, `features/agent/agent-transcript-turn-card.tsx`, `features/agent/agent-provider-self-check-panel.tsx`, `features/agent/connection-shell-state.ts`, `features/agent/connection-actions.ts`, `features/agent/transcript-shell-state.ts`, `features/agent/transcript-follow-banner.tsx`, `features/agent/copy-replay-state.ts`, `features/compare/embedded-session-adapter.ts`, `features/compare/embedded-workbench-adapter.ts`, `features/compare/preference-persistence-model.ts`, `features/compare/reproduce-artifacts.ts`, `lib/agent/*` | `/agent` | `features/agent` + `features/compare` | Chat shell stays in Agent; stream transport and event reduction live in `chat-actions.ts`, submit/replay/resume/tool-decision orchestration lives in `turn-lifecycle.ts`, secondary analysis composition lives in `secondary-analysis-panel.tsx`, runtime and connection state/actions enter through feature-owned shell and composition ports, and runtime phase/stage/guardrail/switch-history projection lives in `runtime-view-model.ts`. Target profile, sidebar identity/tool registry, and runtime-rail props assembly are feature-owned; embedded Compare preference/reproduce/workbench wiring crosses Agent through feature-owned ports. |
| `/compare` | Foreground Compare Studio route with route-owned target sync, preferences, state/actions, recipe orchestration, and shell prop assembly. | `app/compare/page.tsx`, `features/compare/CompareStudioShell.tsx`, `features/compare/CompareRouteWorkbench.tsx`, `features/compare/CompareWorkbench.tsx`, `features/compare/CompareWorkbenchShell.tsx`, `features/compare/components/*`, `features/compare/CompareWorkbenchSidebar.tsx`, `features/compare/workbench-state-model.ts`, `features/compare/workbench-orchestration-model.ts`, `features/compare/workbench-shell-props.ts`, `features/compare/*` | `/compare` | `features/compare` | Product entry no longer borrows `AgentWorkbench`; runtime workbench and composer/review/lane/recipe components live under `features/compare`. The zero-caller `components/agent/AgentCompareLab.tsx`, `AgentRecipeGallery.tsx`, and `components/agent/compare/*` compatibility exports were removed on 2026-07-11. |
| `/fine-tune` | Foreground Fine-tune Studio entry for Train, Evaluate & Predict, Chat Adapter, Export, reports, and artifacts. | `app/fine-tune/page.tsx`, `features/finetune/FineTuneStudioShell.tsx`, `features/finetune/FineTuneStudioPanel.tsx`, `features/finetune/composers/*`, `components/finetune/panels/*` | `/fine-tune` | `features/finetune` | Product entry imports the feature shell directly; shell, composition panel, setup/run/evidence composers, state/actions, preview builders, submit handlers, caches, preset orchestration, job actions, and adapter proof-loop orchestration are feature-owned. |
| `/models` | Foreground model catalog and community install entry. | `app/models/page.tsx`, `app/api/models/discovery/route.ts`, `features/models/ModelsStudioShell.tsx`, `features/models/ModelDiscoveryPanel.tsx`, `features/models/contracts.ts`, `features/models/application.ts`, `components/admin/AdminModelDiscoveryPanel.tsx` | `/models` | `features/models` | Product route now imports the renamed model discovery/install panel from `features/models`; frontend calls `/api/models/discovery`, admin keeps a compatibility re-export, and model discovery API behavior delegates to `features/models/application.ts`. |
| `/benchmarks` | Foreground benchmark history, run controls, and release-evidence entry. | `app/benchmarks/page.tsx`, `features/benchmark/BenchmarksStudioShell.tsx`, `features/benchmark/AdminBenchmarkHistoryPanel.tsx`, `features/benchmark/AdminBenchmarkReleaseEvidencePanel.tsx`, `features/benchmark/release-evidence-formatters.ts`, `features/benchmark/contracts.ts`, `features/benchmark/application.ts`, `lib/agent/benchmark-*` | `/benchmarks` | `features/benchmark` | Product route owns benchmark configuration, execution, progress, and review. Admin now uses feature-owned handoff, release-evidence, and history governance panels instead of assembling those benchmark surfaces itself. |
| `/retrieval` | Foreground knowledge management and grounded retrieval validation. | `app/retrieval/page.tsx`, `features/retrieval/RetrievalStudioShell.tsx`, `features/retrieval/contracts.ts`, `features/retrieval/actions.ts`, `features/retrieval/application.ts`, `features/retrieval/query-application.ts` | `/retrieval` | `features/retrieval` | Product route owns document CRUD, path import/probe, chunk inspection, and retrieval validation. Admin now renders only a governance handoff. |
| `/experiments` | Unified experiment/session timeline, lineage navigation, filters, and retention. | `app/experiments/page.tsx`, `features/experiments/ExperimentsStudioShell.tsx`, `features/experiments/ExperimentTimelinePanel.tsx`, `features/experiments/application.ts`, `features/experiments/retention-service.ts`, `features/experiments/navigation.ts` | `/experiments` | `features/experiments` | Foreground route owns timeline review and retention. Admin uses a compatibility panel over the same feature-owned timeline source. |
| `/agent/full` | Full-size Agent variant. | `components/agent/AgentWorkbench.tsx` | `/agent` responsive mode | `features/agent` | Replace route fork with layout mode when shell is smaller. |
| `/agent/rescue` | Rescue/fallback Agent route. | Agent rescue shell. | `/admin/runtime` or diagnostics | `features/runtime` + `features/admin` | Keep only if needed for recovery; otherwise fold into admin diagnostics. |
| `/admin` | Dashboard, product handoffs, provider health, runtime guardrails, workspace governance, queues, and audit timeline. | `components/admin/*`, `features/admin/AdminFeatureHandoffPanel.tsx`, `features/admin/dashboard-read-model.ts`, `features/admin/runtime-operations.ts`, `features/admin/runtime-guardrail-controller.ts`, `features/admin/compatibility-route.ts`, `features/governance/WorkspaceGovernancePanel.tsx`, `features/benchmark/AdminBenchmarkHandoffPanel.tsx`, `lib/agent/*` | Split across product and admin routes. | `features/admin` + `features/governance` | Fine-tune, Compare, Models, Benchmarks, and Retrieval have foreground entries. Product state/actions no longer live in Admin; runtime policy and workspace governance enter through feature-owned controllers/panels. |
| `/admin/full` | Full-size admin variant. | Admin shell. | `/admin` responsive mode | `features/admin` | Replace route fork with layout mode if possible. |
| `/admin/rescue` | Admin rescue console. | `AdminRescueConsole` | `/admin/runtime` diagnostics | `features/runtime` + `features/admin` | Keep for recovery and health checks. |
| `/focus` | Focus workflow. | `components/focus/*` | `/focus` | `features/focus` | Low priority for this refactor unless it touches sessions. |
| `/inbox` | Inbox workflow. | `components/inbox/*` | `/inbox` | `features/inbox` | Low priority for this refactor. |
| `/item/[id]` | Item details. | `components/item/*` | `/item/[id]` | `features/items` | Low priority for this refactor. |
| `/session-summary` | Session summary. | `components/session/*`, session store | `/experiments` or `/sessions` | `features/experiments` | Should become part of unified experiment/session history. |

## Current API Matrix

| API Route | Current Role | Future Owner | Target Shape |
| --- | --- | --- | --- |
| `/api/agent/chat` | Agent chat transport, provider/runtime/retrieval orchestration. | `features/agent/chat-application` | Seven-line route delegates the complete non-stream application handler to the Agent feature. |
| `/api/agent/chat/stream` | NDJSON Agent chat streaming, protected-tool and retrieval orchestration. | `features/agent/chat-stream-application` | Seven-line route owns only Next runtime configuration and transport delegation. |
| `/v1/chat/completions` | Public OpenAI-compatible completion transport over the real target catalog and provider/runtime router. | `features/providers/openai-compatible-application`, `features/providers/provider-port` | Thin route; bearer auth is optional by deployment policy, workspace tools are disabled, and both JSON and SSE responses come from real inference rather than synthetic completions. |
| `/v1/models` | Public OpenAI-compatible target discovery. | `features/providers/openai-compatible-application`, `lib/agent/catalog` | Lists resolvable model and provider target identifiers without exposing credentials. |
| `/api/agent/compare` | Compare lane execution, local runtime prewarm/recovery, retrieval, provider calls. | `features/compare/application` | Thin route over `CompareRunService`; runtime/provider handled through ports. |
| `/api/compare/progress` | Canonical Compare lane progress read/update transport. | `features/compare/progress-application` | Product-facing GET/POST route; `/api/agent/compare/progress` is a thin compatibility re-export. |
| `/api/compare/recipes` | Canonical Compare recipe list/save/import/delete transport. | `features/compare/recipe-application` | Compare Studio uses the feature route directly; `/api/agent/recipes` remains a thin embedded-Agent compatibility re-export. |
| `/api/agent/connection-check` | Remote provider health probe and evidence logging. | `features/providers/connection-application` | Thin route re-export; provider-health timeline production lives behind the provider/application boundary. |
| `/api/agent/recipes` | Embedded Agent compatibility path for Compare recipes. | `features/compare/recipe-application` | Thin compatibility re-export; no persistence or validation logic remains in the Agent route. |
| `/api/agent/runtime` | Runtime status. | `features/agent/runtime-status-application`, `features/agent/runtime-target-resolution` | Thin route over feature-owned runtime status orchestration and target/env/phase resolution. |
| `/api/agent/runtime/actions` | Runtime prewarm/release/restart/log actions. | `features/agent/runtime-action-application` | Seven-line route delegates action validation, gateway calls, and normalized runtime responses to the Agent feature. |
| `/api/agent/workspace-file` | Read-only workspace file preview. | `features/agent/workspace-file-application` | Thin route over feature-owned path validation and bounded file reads. |
| `/api/agent/check-history` | Connection-check history read model. | `features/agent/check-history-application` | Thin route over feature-owned history normalization. |
| `/api/agent/check-history/export` | Connection-check history export. | `features/agent/check-history-application` | Thin route over feature-owned JSON export assembly. |
| `/api/agent/tool/decision` | Protected-tool approve/reject continuation. | `features/agent/tool-decision-application` | Thin route over feature-owned validation plus local/remote decision execution. |
| `/api/agent/sessions` | Session snapshot and conflict behavior. | `features/agent` then `features/experiments` history | Client persistence and server sync transport now go through `features/agent/session-persistence.ts`; keep shrinking the route into a repository/service wrapper before unified experiment history owns cross-surface snapshots. |
| `/api/agent/targets` | Target catalog. | `features/models` + `features/providers` | Model/provider catalog read API. |
| `/api/benchmarks` | Canonical Benchmark run orchestration and route metadata. | `features/benchmark/run-application` | Foreground Benchmark, Compare, and Fine-tune handoffs use this product-facing route. |
| `/api/benchmarks/baseline` | Canonical Benchmark baseline read/save/rename/default/delete transport. | `features/benchmark/baseline-application` | Foreground route over the feature application. |
| `/api/benchmarks/progress` | Canonical Benchmark progress polling and stop/abandon controls. | `features/benchmark/progress-application` | Foreground GET/POST route; runner evidence links point here. |
| `/api/benchmarks/prompt-sets` | Canonical managed Benchmark prompt-set transport. | `features/benchmark/prompt-set-application` | Benchmark Studio loads and edits prompt sets through this route. |
| `/api/benchmarks/report` | Canonical Benchmark report preview/export. | `features/benchmark/report-application` | Runner report evidence links point here. |
| `/api/benchmarks/evidence` | Canonical pinned Benchmark release evidence. | `features/benchmark/release-evidence-application` | Product and release surfaces read the same feature-owned summary. |
| `/api/benchmarks/export` | Canonical Benchmark history and issue-summary export. | `features/benchmark/export-application` | Benchmark Studio export links use this product route. |
| `/api/benchmarks/qualification` | Official benchmark snapshot qualification and durable reverify. | `features/benchmark/qualification-application`, `features/benchmark/qualification-service` | Fixed-source operator action pins, validates, hashes, and persists MATH-500; scoring and full-run evidence are separate contracts. |
| `/api/benchmarks/official-runs` | Start, resume, and inspect checkpointed full official benchmark runs. | `features/benchmark/official-run-application`, `features/benchmark/official-run-service` | Loopback/operator-only mutations launch the local detached runner; GET separates 500/500 Math-Verify evidence from production promotion. |
| `/api/benchmarks/reproducibility` | Analyze the complete MATH-500 run and replay stored outputs through the pinned scorer. | `features/benchmark/reproducibility-application`, `features/benchmark/reproducibility-service`, `features/benchmark/math500-run-analysis`, `features/benchmark/math500-replay-service` | GET exposes scorecards and readiness; operator POST replays predictions without new model inference and preserves the independent-host boundary. |
| `/api/benchmarks/decision-intelligence` | Build item-level audit, statistical power, and paired candidate promotion evidence from complete MATH-500 runs. | `features/benchmark/decision-intelligence-application`, `features/benchmark/decision-intelligence-service` | Duplicate snapshots with one run id are deduplicated; only distinct complete runs enter McNemar, paired confidence, non-inferiority, and latency gates. |
| `/api/experiments/v163-benchmark-qualification` | Durable 15-slice v1.6.3 qualification acceptance. | `features/experiments/v163-benchmark-qualification` | Experiments read-model mirrors the verified snapshot and preserves local-pass versus production-hold truth. |
| `/api/experiments/v164-official-evaluators` | Pinned evaluator conformance and multimodal protocol evidence. | `features/experiments/v164-official-evaluators` | Records Math-Verify and four multimodal protocol adapters while keeping full multimodal execution and production promotion on HOLD. |
| `/api/experiments/v165-benchmark-reproducibility` | Durable 15-slice run-reproducibility and multimodal-readiness acceptance. | `features/experiments/v165-benchmark-reproducibility` | Binds scorecards, digests, scorer replay, execution plans, and fail-closed truth labels into one local PASS / production HOLD receipt. |
| `/api/experiments/v166-benchmark-decision-intelligence` | Durable 15-slice audit and model-promotion-policy acceptance. | `features/experiments/v166-benchmark-decision-intelligence` | Local audit can pass while candidate promotion remains evidence-needed and production remains HOLD. |
| `/api/experiments/v170-benchmark-candidate-multimodal` | Durable candidate-binding and official multimodal admission gate. | `features/experiments/v170-benchmark-candidate-multimodal` | A new run id does not qualify as a candidate unless its model or adapter binding differs; official image/video execution remains HOLD until assets and native receipts exist. |
| `/api/experiments/post-ga-operations-train` | Read-only v2.1.0–v2.1.9 external operations-attestation chain. | `features/experiments/post-ga-operations-train` | Validates pinned external signatures, closure/predecessor digest binding, freshness, semantic controls, and distinct final reviewer; it cannot operate or authorize production. |
| `/api/experiments/continuous-assurance-train` | Read-only v2.2.0-v2.2.9 continuous compliance and customer-trust chain. | `features/experiments/continuous-assurance-train`, `features/experiments/external-assurance-chain` | Strict schema, signature/pin, freshness, predecessor, semantic coverage, and independent-review verification; no local compliance or production transition. |
| `/api/experiments/assurance-closure-train` | Read-only v2.3.0-v2.3.4 assurance portability and closure chain. | `features/experiments/assurance-closure-train`, `features/experiments/external-assurance-chain` | Verifies portable evidence, publication, monitoring, remediation, and immutable-archive attestations without operating those external systems. |
| `/api/experiments/ai-operations-intelligence` | Read-only v2.4.0-v2.4.9 AI operations signal and external-review chain. | `features/experiments/ai-operations-intelligence-train`, `features/experiments/operational-source-signals`, owning Runtime/Provider/Benchmark/Retrieval/Agent/Workflow/Fine-tune modules | Aggregates real source-owned signals with exception-safe `pass/attention/unavailable` states, then verifies separately signed external records; no local transition. |
| `/api/experiments/deployment-lifecycle-assurance` | Read-only v2.5.0-v2.5.4 deployment lifecycle signal and closure chain. | `features/experiments/deployment-lifecycle-assurance-train`, `features/experiments/operational-source-signals`, `features/deployment/control-plane` | Projects portability, sovereignty, key, continuity, and exit signals beside independent customer/operator evidence; local signing never substitutes for cloud or customer authority. |
| `/api/experiments/governed-autonomy-readiness` | Read-only v2.6.0-v2.6.9 autonomy policy and independent-review chain. | `features/experiments/governed-autonomy-readiness-train`, `features/experiments/governed-autonomy-source-signals`, owning Models/Provider/Retrieval/Extension/Agent/Workflow/Benchmark/Fine-tune/Governance modules | Projects real feature-owned controls into exception-safe local signals, then verifies separately signed external records; no execution or production transition. |
| `/api/experiments/open-ecosystem-interoperability` | Read-only v2.7.0-v2.7.4 client, extension, artifact, identity, and closure chain. | `features/experiments/open-ecosystem-interoperability-train`, `features/experiments/governed-autonomy-source-signals`, owning Runtime/Extension/Models/Governance modules | Verifies local interoperability evidence beside independent client, publisher, operator, and identity records; missing external authority remains BLOCKED. |
| `/api/experiments/operational-remediation-efficiency` | Read-only v2.8.0-v2.8.9 provider, Retrieval, supply-chain, audit, runtime, Agent, Workflow, Benchmark, Fine-tune, and review train. | `features/experiments/operational-remediation-efficiency-train`, `features/experiments/operational-sustainability-source-signals`, owning Provider/Retrieval/Models/Governance/Agent/Workflow/Benchmark/Fine-tune modules | Projects owner-controlled remediation evidence without running workloads; local gaps remain attention and external authority remains BLOCKED. |
| `/api/experiments/sustainable-operations-upgrade` | Read-only v2.9.0-v2.9.4 telemetry, diagnostics, compatibility sunset, desktop lifecycle, and closure train. | `features/experiments/sustainable-operations-upgrade-train`, `features/experiments/operational-sustainability-source-signals`, owning Telemetry/Agent/Provider/Admin/Desktop modules | Exposes operational sustainability and upgrade evidence without deleting wrappers, changing services, exporting traces, or authorizing production. |
| `/api/experiments/remediation-control` | Read-only v3.0.0-v3.0.9 owner remediation, dependency graph, evidence package, and independent acceptance train. | `features/experiments/remediation-control-train`, `features/experiments/service-readiness-source-signals`, `features/experiments/operational-remediation-control-plane`, existing feature owners | Projects owner, priority, blocked-by, acceptance, next-action, and evidence-fingerprint contracts without executing workloads or changing authority. |
| `/api/experiments/service-readiness` | Read-only v3.1.0-v3.1.4 disclosure, support diagnostics, upgrade/change, transition, and independent closure train. | `features/experiments/service-readiness-train`, `features/experiments/service-readiness-source-signals`, remediation control plane and existing Support/Admin/Desktop owners | Keeps service readiness attention while upstream remediation is open; local code cannot sign closure or authorize production. |
| `/api/experiments/remediation-execution` | Read-only v3.2.0-v3.2.9 owner execution, lease/fencing, rollback package, and independent execution acceptance train. | `features/experiments/remediation-execution-train`, `features/experiments/remediation-execution-source-signals`, remediation control plane and existing feature owners | Produces deterministic local execution plans only; remote mutation remains disabled and source status follows upstream owner evidence. |
| `/api/experiments/operational-acceptance` | Read-only v3.3.0-v3.3.4 SLO, rehearsal, owner sign-off, release decision, and independent operational acceptance train. | `features/experiments/operational-acceptance-train`, `features/experiments/remediation-execution-source-signals`, existing Support/Desktop/Release owners | Preserves predecessor order and explicit HOLD/BLOCKED authority; local code cannot approve distribution or production. |
| `/api/experiments/owner-workload-admission` | Read-only v3.4.0-v3.4.9 owner workload admission train plus strict candidate receipt validation. | `features/experiments/owner-workload-admission-application`, `features/experiments/owner-workload-protocol`, existing feature owners | `GET` projects requests and evidence; `POST` validates schema/digest/time/identity only. Neither path executes remote mutations or grants release authority. |
| `/api/experiments/operational-decision-governance` | Read-only v3.5.0-v3.5.4 freshness, dependency impact, owner SLA, bounded waiver, and independent decision train. | `features/experiments/operational-decision-governance-train`, `features/experiments/owner-workload-source-signals` | Keeps stale, incomplete, or externally unsigned evidence on HOLD and production BLOCKED. |
| `/api/experiments/owner-receipt-lifecycle` | v3.6.0-v3.6.9 candidate receipt intake, quarantine ledger, compensation reconciliation, and independent closure train. | `features/experiments/owner-receipt-lifecycle-application`, `features/experiments/owner-receipt-lifecycle`, `features/experiments/owner-receipt-intake-train` | `GET` is a fail-closed read model; loopback/operator-authenticated `POST` appends digest-only events with optimistic concurrency. Raw receipt payloads and production authority are never persisted. |
| `/api/experiments/operational-exception-lifecycle` | v3.7.0-v3.7.4 SLA breach, acknowledgement, bounded waiver, decision package, and independent exception closure train. | `features/experiments/owner-receipt-lifecycle-application`, `features/experiments/operational-exception-lifecycle-train` | Operator-authenticated mutations append acknowledgement or waiver events; protected scopes, stale revisions, renewal, and production overrides fail closed. |
| `/api/admin/benchmark` | Admin compatibility entry for Benchmark run orchestration. | `features/benchmark/run-application` | Thin re-export retained for Admin rescue and historical callers while product surfaces use `/api/benchmarks`. |
| `/api/admin/benchmark/baseline` | Admin compatibility baseline transport. | `features/benchmark/baseline-application` | Thin re-export over the canonical feature application. |
| `/api/admin/benchmark/progress` | Admin compatibility progress transport. | `features/benchmark/progress-application` | Thin re-export over the canonical feature application. |
| `/api/admin/benchmark/prompt-sets` | Admin compatibility prompt-set transport. | `features/benchmark/prompt-set-application` | Thin re-export over the canonical feature application. |
| `/api/admin/benchmark/report` | Admin compatibility report transport. | `features/benchmark/report-application` | Thin re-export over the canonical feature application. |
| `/api/admin/benchmark/evidence` | Admin compatibility release-evidence transport. | `features/benchmark/release-evidence-application` | Thin re-export over the canonical feature application. |
| `/api/admin/benchmark/export` | Admin compatibility export transport. | `features/benchmark/export-application` | Thin re-export over the canonical feature application. |
| `/api/admin/dashboard` | Admin metrics aggregation. | `features/admin/dashboard-application` | Thin route re-export over the Admin-only read model. |
| `/api/finetune` | Canonical Fine-tune dataset/recipe/job/report/bundle/eval/chat/export transport. | `features/finetune/application` | Foreground route and action clients use this path; summary smoke protects the full shape. |
| `/api/admin/finetune` | Deprecated Fine-tune compatibility transport. | `features/admin/compatibility-route` + `features/finetune/application` | Returns `Deprecation`, `Sunset`, `Link`, and `Warning` headers pointing to `/api/finetune`. |
| `/api/admin/knowledge-base` | Deprecated knowledge CRUD/query compatibility transport. | `features/admin/compatibility-route` + `features/retrieval/*application` | Returns compatibility headers pointing to `/api/retrieval` and `/api/retrieval/query`. |
| `/api/retrieval` | Foreground knowledge CRUD/import transport. | `features/retrieval/application` | Canonical thin route for `/retrieval`; admin path remains compatibility-only. |
| `/api/retrieval/query` | Foreground grounded retrieval validation. | `features/retrieval/query-application` | Canonical thin query route for `/retrieval`. |
| `/api/retrieval/enterprise` | Strict enterprise ingestion/query readiness, pgvector migration, hybrid retrieval, cross-encoder reranking, citations, and ACL context. | `features/retrieval/enterprise-service` | Fails closed when PostgreSQL, embedding, or reranker services are absent; it never silently substitutes the local hash retriever. |
| `/api/telemetry` | OpenTelemetry/Langfuse readiness and local span evidence. | `features/telemetry/trace-adapter` | Provider, Fine-tune, Workflow, and Retrieval spans share one batched OTLP adapter; external exporter acceptance remains a deployment gate. |
| `/api/models/discovery` | Foreground model discovery/install/verify transport for `/models`. | `features/models/application` | Thin route re-export over the feature-owned model discovery handlers; `/api/admin/model-discovery` remains a compatibility mirror. |
| `/api/admin/model-discovery` | Deprecated model discovery compatibility transport. | `features/admin/compatibility-route` + `features/models/application` | Returns compatibility headers pointing to `/api/models/discovery`. |
| `/api/admin/runtime-guardrail` | Runtime safety policy configuration. | `features/runtime/application` | Admin policy editor; product consumes read-only risk results. |
| `/api/admin/timeline` | Deprecated experiment timeline compatibility transport. | `features/admin/compatibility-route` + `features/experiments/application` | Returns compatibility headers pointing to `/api/experiments`; product history and Admin audit still share the same feature-owned source. |
| `/api/experiments` | Foreground timeline read and retention control. | `features/experiments/application` | Canonical thin route for timeline filters/navigation/retention; admin route is compatibility-only. |
| `/api/desktop/readiness` | First-run host, runtime, storage, package, and background-service diagnostics. | `features/desktop/first-run-readiness` | Read-only machine diagnostic contract; signed package and service evidence remain explicit watch states. |
| `/api/desktop/package-rehearsal` | Signed package manifest and install/upgrade/rollback/uninstall rehearsal. | `features/desktop/package-rehearsal` | Local Ed25519 receipts are kept separate from Apple Developer ID `codesign` evidence. |
| `/api/desktop/update-channel` | Durable stable/preview update and rollback-channel rehearsal. | `features/desktop/update-channel` | Local Ed25519 stage/activate/rollback receipts validate the channel contract without claiming Apple notarization. |
| `/api/desktop/data-lifecycle` | User-data migration, uninstall-preserve, explicit purge, and backup/restore policy. | `features/desktop/data-lifecycle` | Executes only in an isolated fixture; clean-machine signed-package evidence remains external. |
| `/api/desktop/service-supervisor` | Durable background-service state, heartbeat, degradation, and recovery generation. | `features/desktop/service-supervisor` | Validates supervisor policy without installing or claiming a live launchd service. |
| `/api/desktop/permission-repair` | Approved-root permission diagnosis and symlink-safe repair rehearsal. | `features/desktop/permission-repair` | Mutates only an isolated fixture; user directories and macOS privacy settings remain untouched. |
| `/api/desktop/apple-release-signing` | Developer ID preflight and production app/DMG notarization execution. | `features/desktop/apple-release-signing` | Signs nested Mach-O code and app before ZIP/DMG notarization; preflight and Accepted/stapled/Gatekeeper-complete evidence are separate states. |
| `/api/desktop/external-acceptance` | Independent Mac and organization acceptance read-model. | `features/desktop/external-acceptance` | Requires a different host fingerprint, complete checks, RSA-SHA256 receipt verification, and an out-of-band pinned organization public-key digest. |
| `/api/desktop/onboarding-release` | v1.1 first-run, package, local-chat, migration, service, update, and external-signing projection. | `features/desktop/onboarding-release` | Canonical Desktop release read-model and rehearsal orchestrator; local RC readiness is reported separately from Developer ID/notarization GA readiness. |
| `/api/models/acquisitions` | Durable model acquisition job creation, pause/resume/cancel, and bounded transfer lifecycle. | `features/models/model-acquisition` | Executes HTTP Range chunks into `.part` files, records progress/ETag/range, verifies SHA-256, and atomically materializes the destination. |
| `/api/models/content-deduplication` | Content-addressed model dedup plan and isolated atomic hardlink rehearsal. | `features/models/content-deduplication` | Real model files remain plan-only until an operator-approved maintenance window. |
| `/api/models/hub-transfers/reconcile` | Hub session/file/checksum/retry reconciliation. | `features/models/hub-session-reconciliation` | Completed files fail closed on missing path or SHA metadata; exhausted retries remain visible findings. |
| `/api/models/external-storage` | External-storage planning, fixture rehearsal, and operator-approved physical-volume migration. | `features/models/external-storage-migration` | Physical mode requires `/Volumes`, external/writable identity, capacity, per-file SHA-256, same-volume staging and atomic rename; source deletion needs a second approval phrase. |
| `/api/models/promotion-evidence` | Authenticated Hub transfer plus physical-storage promotion read model. | `features/models/model-hub-promotion-evidence` | Promotion passes only when immutable revision, token identity digest, every downloaded SHA-256, migration source path, ownership file set, directory digest, and physical volume UUID bind together. |
| `/api/models/compatibility` | Model format, runtime, license, tokenizer, chat-template, quantization, and hardware-fit manifest. | `features/models/compatibility-manifest` | Install and benchmark handoffs fail closed when a required compatibility check is unresolved. |
| `/api/models/benchmark-handoff` | Compatibility-gated Model Hub to Benchmark request. | `features/models/benchmark-handoff` | Emits a benchmark request only when the linked compatibility receipt is passing. |
| `/api/models/source-manifests` | Pinned authenticated Hugging Face/ModelScope multi-file manifest validation. | `features/models/source-manifest` | Persists only token presence plus public file metadata; this route does not claim a real hub request. |
| `/api/models/transfer-scheduler` | Global/per-host concurrency, priority, retry-backoff, and cancellation scheduling. | `features/models/transfer-scheduler` | Produces a deterministic bounded transfer plan independently from download transport. |
| `/api/models/removal-lifecycle` | Quarantine, rollback, shared-blob ownership, and orphan-cleanup lifecycle. | `features/models/removal-lifecycle` | Executable evidence uses an isolated hardlink fixture and never deletes installed models. |
| `/api/models/server-instances` | Durable local server instances, idle policy, network exposure, and auth policy. | `features/models/server-instance-registry` | Rejects unauthenticated LAN registration and owns the durable instance state. |
| `/api/models/server-instances/actions` | Executable register/hot-switch/unload lifecycle actions. | `features/models/server-lifecycle` | Ollama actions execute through the runtime adapter and persist lifecycle receipts plus active model state. |
| `/api/models/local-server-acceptance` | Real Ollama 15-slice lifecycle, API, concurrency, accounting, policy, eviction, and recovery acceptance. | `features/models/local-server-acceptance` | POST runs a real local model workload and persists per-slice evidence; GET is read-only and remains valid when no local receipt exists. |
| `/api/models/local-server-promotion` | v1.2.0 local-versus-production promotion read model. | `features/models/local-server-promotion-evidence` | Local 15/15 evidence is reported separately from authenticated separate-device LAN and sustained daemon-window gates. |
| `/api/models/server-instances/idle-unload` | Bounded idle-unload daemon decision tick. | `features/models/idle-unload-daemon` | Defaults to dry-run; execution requires an explicit action and records each per-server decision. |
| `/api/models/server-instances/access` | Server caller-key issuance, digest-only verification, revocation, and request attribution. | `features/models/server-access-control`, `features/models/server-request-ledger` | Plaintext keys are returned only at issue time; durable state keeps a SHA-256 digest and ledger caller identity. |
| `/api/models/server-instances/network-policy` | Trusted-host, CORS, API-key, and rate-limit policy decisions. | `features/models/server-network-policy` | LAN exposure fails closed unless every required host, origin, key, and quota check passes. |
| `/api/models/server-instances/switch-controller` | In-flight drain, candidate health, activation, and rollback policy. | `features/models/server-switch-controller` | State-machine evidence is separate from live runtime traffic and stress evidence. |
| `/api/models/server-instances/log-retention` | Request-log retention, caller redaction, and digest-backed export. | `features/models/server-log-retention` | Expired records are excluded and caller key ids become one-way aliases. |
| `/api/runtime/adapters` | Backend-neutral runtime capability and conformance read-model. | `features/runtime/adapter-conformance` | All six adapters are implemented; live availability remains separate from contract conformance. |
| `/api/runtime/ollama` | Live Ollama health, discovery, prewarm, and unload bridge. | `features/runtime/ollama-adapter` | Unavailable runtimes return normalized evidence; live model conformance remains required before promotion. |
| `/api/runtime/fleet-conformance` | Registered-server OpenAI-compatible conformance projection. | `features/runtime/fleet-conformance` | Active models require fresh passing evidence; servers without an active model remain informational. |
| `/api/runtime/fabric-acceptance` | Real three-backend normalized acceptance. | `features/runtime/runtime-fabric-acceptance` | MLX, Ollama, and llama.cpp must pass health, discovery, chat, stream, and usage normalization. |
| `/api/runtime/fabric-promotion` | Runtime Fabric local/production promotion split. | `features/runtime/runtime-fabric-promotion` | Local three-backend evidence can pass while external LocalAI/Linux-NVIDIA/remote-node receipts remain HOLD. |
| `/api/runtime/operation-port` | Normalized health/discovery/chat/stream/prewarm/unload/cancel operations across six runtime backends. | `features/runtime/operation-port` | Every backend returns a normalized success or actionable unsupported result. |
| `/api/runtime/remote-nodes` | Remote-node registration, capability, memory, residency, and state-aware routing. | `features/runtime/remote-node-registry` | Loopback is accepted for rehearsal only; non-loopback nodes require HTTPS and real multi-machine evidence remains open. |
| `/api/runtime/remote-failover` | Heartbeat lease expiry, standby selection, and fencing generation. | `features/runtime/remote-failover` | Deterministic local evidence does not count as real multi-machine failover or RPO/RTO proof. |
| `/api/extensions` | Signed extension manifest, permission, trust verification, receipts, and quarantine. | `features/extensions/registry`, `features/extensions/package-verification` | Invalid or untrusted bytes are non-executable and quarantined. |
| `/api/extensions/mcp-servers` | Pinned MCP server registration, integrity verification, enable/disable, and probe receipts. | `features/extensions/mcp-server-registry` | Only the allowlisted official filesystem package and lockfile-pinned version/integrity are accepted. |
| `/api/extensions/acceptance` | Real signed lifecycle, package-defense, OS-sandbox, and MCP stdio acceptance. | `features/extensions/extension-ecosystem-acceptance` | POST executes the local workload; GET returns durable receipts without turning local evidence into production proof. |
| `/api/extensions/promotion` | v1.3.0 local-versus-production promotion read model. | `features/extensions/extension-ecosystem-promotion` | Local PASS remains separate from publisher, Linux/Windows isolation, and remote OAuth production receipts. |
| `/api/extensions/installations` | Verified extension atomic install, update, activation, disable/enable, and rollback. | `features/extensions/install-transaction` | Signed bundles expand in staging, enforce safe paths and sandbox policy, then activate by atomic rename with durable lifecycle receipts. |
| `/api/extensions/secret-scope` | Exact extension/secret/operation/expiry/confirmation authorization decisions. | `features/extensions/secret-scope-policy` | The evidence records policy metadata only and never persists secret values. |
| `/api/extensions/permission-grants` | Time/scoped extension grants, confirmation, authorization, and revocation. | `features/extensions/permission-grants` | Dangerous permissions require explicit confirmation and revoked grants fail closed. |
| `/api/extensions/quarantine-review` | Signature/sandbox/dependency-gated quarantine review. | `features/extensions/quarantine-review` | Release requires every safety check plus operator and reason evidence. |
| `/api/workflows` | Visual graph draft mutation, strict validation, optimistic revision control, immutable publish, breakpoints, and persisted execution actions. | `features/workflows/graph-contract`, `features/workflows/editor-state`, `features/workflows/graph-registry`, `features/workflows/execution-reducer` | Published versions are immutable; stale draft revisions fail closed; protected side effects require approval and idempotency. |
| `/api/workflows/deploy/[slug]/v1/chat/completions` | OpenAI-compatible durable workflow invocation. | `features/workflows/deployment-application` | Accepts pinned deployed graph versions and returns an explicit asynchronous workflow acceptance contract; streaming remains fail-closed. |
| `/api/workflows/acceptance` | v1.3.1 graph-authoring, versioning, recovery, replay, deployment and standard completion/SSE rehearsal. | `features/workflows/studio-acceptance` | POST requires the operator boundary and retains a durable digest-backed local receipt; local evidence cannot satisfy non-loopback auth or distributed-worker production gates. |
| `/api/workflows/promotion` | v1.3.1 local-versus-production promotion read model. | `features/workflows/studio-promotion` | Local PASS remains separate from authenticated non-loopback invocation, distributed recovery, and collaborative conflict evidence. |
| `/api/workflows/worker` | Leased execution of replay-safe workflow nodes. | `features/workflows/worker-service` | The worker releases durable leases and stops before approvals, breakpoints, or write/external side effects. |
| `/api/admin/orchestration-shadow` | Protected-tool interrupt/resume shadow-graph evidence. | `features/workflows/langgraph-shadow-service` | Executes a real LangGraph graph with a checkpointer and resume command, and records duplicate-side-effect evidence without replacing the production workflow reducer. |
| `/api/workflows/replay` | Same-version workflow replay fork. | `features/workflows/replay-service` | Replays preserve graph/input lineage and intentionally copy no side-effect history. |
| `/api/workflows/state-diff` | Source/fork state diff and persisted-breakpoint replay evidence. | `features/workflows/state-diff` | Fork-time event and idempotency histories must be empty; replay must not mutate the source execution. |
| `/api/workflows/retrieval-graph` | Versioned Retrieval graph publication, deployment slug, and worker execution. | `features/workflows/retrieval-graph`, `features/workflows/graph-registry` | Publishes an immutable graph version and exercises replay-safe retrieval/model/evaluator steps. |
| `/api/workflows/deployment-access` | Digest-backed workflow keys pinned to slug, immutable version, and scope. | `features/workflows/deployment-access` | Plaintext is returned only when issued and is never exposed by the evidence read model. |
| `/api/governance` | Organization/workspace identity, SQL-enforced ACL read-model, and operator-only isolation/mapping/conflict rehearsals. | `features/governance/workspace-identity`, `features/governance/workspace-acl-database`, `features/governance/workspace-request-context`, `features/governance/governance-rehearsal-evidence` | SQLite mapping/conflict and Postgres RLS evidence stay distinct; external organization identity remains deployment evidence. |
| `/api/governance/identity-mappings` | SCIM group to organization/workspace/role binding and local rehearsal. | `features/governance/identity-provisioning`, `features/governance/identity-workspace-mapping` | OIDC subject plus explicit group claim is required; inactive users and cross-organization bindings fail closed. |
| `/api/governance/workspaces/resources` | Workspace-scoped resource list/create/revision update and optional admin audit read. | `features/governance/workspace-request-context`, `features/governance/workspace-acl-database`, `features/governance/postgres-workspace-port` | Thin route over signed request identity, database-enforced organization/workspace/role boundaries, and typed HTTP 409 optimistic conflicts. |
| `/api/governance/policy-simulator` | Role/action/cross-workspace policy simulation. | `features/governance/policy-simulator` | Default owner/builder/viewer and deny scenarios produce durable expectation-matching receipts. |
| `/api/governance/shared-assets` | Shared-asset ACL decisions and hash-chained audit export. | `features/governance/shared-asset-audit` | Cross-workspace reads are denied and exported audit events remain verifiable from genesis through the export digest. |
| `/api/governance/access-reviews` | Time-bounded four-eyes access request and decision workflow. | `features/governance/access-review` | Self approval and unauthorized reviewer roles fail closed; reviewer and reason are durable evidence. |
| `/api/experiments/external-production-readiness` | Consolidated external release-gate projection. | `features/experiments/external-production-readiness` | Keeps OIDC/SCIM, PostgreSQL RLS, enterprise Retrieval, telemetry exporter, and cloud KMS/Object Lock acceptance blocked until independent evidence exists. |
| `/api/finetune/training-capabilities` | Model/backend/method/quantization/scheduler compatibility. | `features/finetune/training-capabilities` | UI and workers can fail closed before offering unsupported recipes. |
| `/api/finetune/training-execution-plan` | Backend-neutral training config and argv materialization. | `features/finetune/training-execution-plan` | MLX-LM plans are worker-ready; preview backends can generate a plan but cannot execute through this boundary. |
| `/api/evaluation/regression-suite` | Multi-metric paired regression gate. | `features/evaluation/regression-suite` | Higher/lower-is-better metrics share confidence-bound and minimum-sample enforcement. |
| `/api/evaluation/quality-ci` | Frozen artifact, multi-seed, confidence, judge-calibration, and reproducible-decision read model. | `features/evaluation/quality-ci-gate` | Deterministic objective protocols may pass locally; subjective evaluation remains fail-closed without calibrated judge evidence. |
| `/api/evaluation/release-candidate` | v1.5.1 real adapter release-candidate acceptance. | `features/evaluation/release-candidate-acceptance` | Binds an exact checkpoint and base revision to paired Benchmark evidence, registry provenance, usage settlement, and local failover; independent worker, remote registry, managed billing, cloud signing/archive, and organization sign-off remain external. |
| `/api/evaluation/sweep-calibration` | Hardware/time-budgeted sweep, early-stop policy, and judge calibration. | `features/evaluation/sweep-calibration` | Selected trials and judge agreement must satisfy explicit budgets and thresholds. |
| `/api/evaluation/baseline-promotion` | Pinned reproducibility manifest, multi-metric comparison, calibration, and baseline promotion. | `features/evaluation/baseline-promotion` | Promotion remains hold unless references, metrics, deterministic seed, and judge threshold all pass. |
| `/api/artifacts/packages` | Unified package manifest for model, adapter, dataset, RAG, eval, profile, and workflow artifacts. | `features/artifacts/package-contract`, `features/artifacts/provenance-gate` | Digest/signature/license/dependency/evidence checks are canonical before publication. |
| `/api/artifacts/acceptance` | Fixed-fixture provenance and local-registry round-trip acceptance. | `features/artifacts/artifact-ecosystem-acceptance` | Internal trust material is scoped to the built-in fixture and cannot authorize caller-supplied packages. |
| `/api/artifacts/registry` | Provenance-approved local artifact registry publication. | `features/artifacts/local-registry` | Package bytes are staged atomically and read back for SHA-256 round-trip verification. |
| `/api/artifacts/registry-adapters` | Fail-closed GitHub, ModelScope, Hugging Face, and private OCI staging plans. | `features/artifacts/registry-adapters` | Preview adapters expose required environment and immutable round-trip stages but never mutate a remote registry. |
| `/api/artifacts/quality-claims` | Evidence-backed artifact quality claim linked to regression and reconciled usage. | `features/artifacts/quality-billing-link` | Claims materialize only from a verified registry record, passing regression evidence, and zero-difference usage reconciliation. |
| `/api/artifacts/install-lifecycle` | Digest-verified install, upgrade, rollback, and inactive-version unpublish. | `features/artifacts/install-lifecycle` | Active versions cannot be unpublished; local fixture evidence is not an external registry round trip. |
| `/api/deployment/usage-reconciliation` | Request-ledger to durable usage-outbox reconciliation. | `features/deployment/usage-reconciliation` | A ledger digest provides idempotency and token differences must be exactly zero. |
| `/api/deployment/usage-settlement` | Durable pending/failure/retry/delivery settlement with idempotency. | `features/deployment/usage-settlement` | Local external receipts validate lifecycle semantics but do not count as a real billing-provider settlement. |
| `/api/experiments/post-v1-foundation` | Ten-version foundation evidence projection. | `features/experiments/post-v1-foundation` | Aggregates domain-owned contracts without promoting planned releases or hiding cloud blockers. |
| `/api/experiments/post-v1-hardening` | Fifteen-slice executable hardening projection. | `features/experiments/post-v1-hardening` | Aggregates domain receipts for `/experiments`; local readiness does not promote full versions or external production gates. |
| `/api/experiments/post-v1-acceptance` | Second fifteen-slice local product-acceptance projection. | `features/experiments/post-v1-acceptance` | Aggregates executable local receipts while preserving Apple, physical-disk, remote-node, identity, and cloud gates as external. |
| `/api/experiments/post-v1-lifecycle` | Third fifteen-slice operational lifecycle projection. | `features/experiments/post-v1-lifecycle` | Aggregates recovery, authorization, rollback, promotion, and settlement receipts without promoting planned releases. |
| `/api/experiments/post-v1-promotion-gate` | Unified v1.1.0-v1.5.1 local and production readiness projection. | `features/experiments/post-v1-promotion-gate` | Local fixtures can satisfy local readiness only; Apple, organization identity, real registry, and cloud evidence stay explicit. |
| `/api/experiments/v14-acceptance` | v1.4 Governance, distributed Workflow, and Quality CI acceptance batch. | `features/experiments/v14-acceptance-batch` | Local identity, lease, conflict, and evaluation evidence cannot replace a real IdP, deployed database, separate workers, or organization acceptance. |
| `/api/experiments/v15-acceptance` | v1.5 trusted artifact and durable accounting acceptance batch. | `features/experiments/v15-acceptance-batch` | Requires the exact passing release-candidate quality receipt for local closure while remote registry, managed billing, cross-region, and cloud evidence remain HOLD. |

## Module Ownership Matrix

### 2026-07-11 Boundary Amendments

- v1.3.0 MCP server registration, filesystem-server acceptance, signed extension ecosystem acceptance, OS sandbox descriptors/receipts, and promotion projection now live under `features/extensions`; `/api/extensions/mcp-servers`, `/acceptance`, and `/promotion` are thin application wrappers.
- Agent shell composition now includes `features/agent/workbench-header.tsx`, `workbench-status-band.tsx`, `workbench-prompt-strip.tsx`, `workbench-composition.ts`, `conversation-selectors.ts`, `session-sync-status.ts`, and the physically owned `get-code-panel.tsx`.
- Agent outer layout now delegates sidebar content, main workspace, and Chat/Compare switching to `workbench-sidebar-content.tsx`, `workbench-main.tsx`, `workbench-mode-content.tsx`, and `workbench-layout.tsx`; `AgentWorkbench.tsx` creates state/actions and one composed layout input.
- Agent session create/rename/pin/delete/bulk-clear/export commands now live in `session-command-actions.ts` and `session-export.ts`; chat submit/replay/resume/tool-decision dependencies enter `turn-lifecycle.ts` through `turn-lifecycle-input.ts`.
- Admin telemetry rendering and formatting now live in `features/admin/telemetry-components.tsx` and `features/admin/runtime-formatters.ts`.
- Admin per-target runtime derivation, hardware charts, and log tooling now live in `runtime-target-view-model.ts`, `AdminRuntimeMetricsGrid.tsx`, and `AdminRuntimeLogPanel.tsx`.
- Admin runtime trace plus model-state/action rendering now live in `AdminRuntimeTracePanel.tsx` and `AdminRuntimeModelStatePanel.tsx`; Benchmark history identity/failure/evidence metadata and Compare run-note details live in `features/benchmark/AdminBenchmarkHistoryEntryHeader.tsx` and `AdminBenchmarkRunNotePanel.tsx`.
- Benchmark heatmap, metric, and failure interpretation now live in `features/benchmark/analysis-formatters.ts` and `features/benchmark/failure-analysis.ts`.
- `/api/agent/runtime` keeps HTTP serialization in the route while `features/agent/runtime-status-application.ts` returns a transport-neutral `{ status, payload }` result.
- Remote runtime status payload construction lives in `features/agent/runtime-remote-status.ts`, separate from local gateway health orchestration.
- Local runtime recovering, ready, and unavailable payload construction lives in `features/agent/runtime-local-status.ts`; `runtime-status-application.ts` now owns only request validation, target resolution, probing, auto-start, and runtime metric collection.
- Agent base target/session/chat/tool-decision/sync state creation now lives in `features/agent/chat-session-state.ts`; `AgentWorkbench.tsx` consumes the state port alongside existing lifecycle and shell composition hooks.
- Benchmark execution grouping and per-result metric cards now live in `features/benchmark/AdminBenchmarkResultGroups.tsx`; Admin recent chats, percentiles, model/context breakdown, connection checks, and observability paths live in `features/admin/AdminRecentOperationsPanel.tsx`.
- Local gateway timeout/probe/auto-ensure/recovering policy now lives in `features/agent/runtime-local-health.ts`, leaving status application independent from both health policy and payload builders.
- Admin compatibility deletion evidence now exposes a 5-route pre-sunset rehearsal separately from the legally timed delete-ready/sign-off gate.
- Agent target catalog polling/fallback, locale-default migration, session-sync projections, and transcript export now live in `target-catalog-sync.ts`, `locale-default-sync.ts`, `session-sync-projections.ts`, and `transcript-export.ts`.
- Admin filter state, dashboard query serialization, Benchmark report links/progress polling, and runtime switch-history persistence now live in `dashboard-filter-state.ts`, `dashboard-query.ts`, `benchmark-report-application.ts`, `benchmark-progress-state.ts`, and `runtime-switch-history.ts`.
- Benchmark coverage governance and heatmap controls/scope/cell rendering now live in `AdminBenchmarkCoverageGovernancePanel.tsx` and `AdminBenchmarkHeatmapPanel.tsx`.
- Compatibility pre-sunset rehearsal exports through `/api/admin/compatibility-usage/rehearsal/export`; screenshot integrity is machine-verified by `scripts/validate-release-screenshots.mjs` and enters the GA bundle through `screenshot-integrity-evidence.ts`.

| Module Area | Current Location | Future Boundary | Notes |
| --- | --- | --- | --- |
| Agent shell and chat | `components/agent/AgentWorkbench.tsx`, `features/agent/chat-actions.ts`, `features/agent/turn-lifecycle.ts`, `features/agent/secondary-analysis-panel.tsx`, `features/agent/session-model.ts`, `features/agent/session-persistence.ts`, `features/agent/session-apply.ts`, `features/agent/session-hydration.ts`, `features/agent/session-server-sync.ts`, `features/agent/session-sidebar-selectors.ts`, `features/agent/workbench-preferences.ts`, `features/agent/workbench-shell-state.ts`, `features/agent/runtime-shell-state.ts`, `features/agent/runtime-actions.ts`, `features/agent/runtime-formatters.ts`, `features/agent/runtime-status-rail.tsx`, `features/agent/runtime-rail-composition.ts`, `features/agent/target-catalog-panel.tsx`, `features/agent/target-profile-panel.tsx`, `features/agent/workbench-sidebar.tsx`, `features/agent/sidebar-composition.ts`, `features/agent/tool-registry-panel.tsx`, `features/agent/agent-composer-form.tsx`, `features/agent/session-tools-panel.tsx`, `features/agent/workspace-file-preview-panel.tsx`, `features/agent/workspace-file-actions.ts`, `features/agent/agent-tool-review-panel.tsx`, `features/agent/agent-tool-run-review-card.tsx`, `features/agent/agent-transcript-panel.tsx`, `features/agent/agent-transcript-turn-card.tsx`, `features/agent/agent-provider-self-check-panel.tsx`, `features/agent/connection-shell-state.ts`, `features/agent/connection-actions.ts`, `features/agent/transcript-shell-state.ts`, `features/agent/transcript-follow-banner.tsx`, `features/agent/copy-replay-state.ts`, `features/agent/runtime-status-application.ts`, `features/agent/runtime-target-resolution.ts`, `features/agent/workspace-file-application.ts`, `features/agent/check-history-application.ts`, `features/agent/tool-decision-application.ts`, `lib/agent/providers.ts` | `features/agent` | Shell should coordinate, not own Compare/Fine-tune internals. Transport/event reduction lives in `chat-actions.ts`; turn submit/replay/resume/tool-decision lifecycle lives in `turn-lifecycle.ts`; secondary analysis composition lives in `secondary-analysis-panel.tsx`. Session persistence and synchronization use feature-owned ports. Sidebar, target profile, runtime rail props, workspace file, check history, protected-tool decisions, and runtime status orchestration now live behind named `features/agent` composition or application boundaries. |
| Compare | `features/compare/CompareWorkbench.tsx`, `features/compare/components/*`, `features/compare/*`, `lib/agent/compare-*` | `features/compare` | Contract facade starts at `features/compare/contracts.ts`; `/compare` uses `CompareRouteWorkbench`, Agent embedded Compare uses session/workbench feature adapters, and session snapshot/apply behavior crosses into Agent only through `CompareSessionPreferencePort`. Old Agent compatibility re-exports have been removed after a zero-caller audit. |
| Fine-tune | `features/finetune/FineTuneStudioShell.tsx`, `features/finetune/FineTuneStudioPanel.tsx`, `features/finetune/composers/*`, `components/finetune/panels/*`, `components/admin/AdminFineTunePanel.tsx`, `lib/finetune/*` | `features/finetune` | Foreground shell/composition and setup/run/evidence composers are physically feature-owned; reusable leaf panels remain presentational components. Admin uses a compact governance handoff and a compatibility wrapper only where required. |
| Benchmark | `features/benchmark/*`, `components/admin/AdminDashboard.tsx`, `lib/agent/benchmark-*`, admin benchmark routes | `features/benchmark` | Product `/benchmarks` owns configuration, execution, progress, history/evidence, and report workflows. Admin delegates handoff, release evidence, history composition, and match-source formatting to feature-owned panels/helpers. `run-application.ts` delegates route execution to `run-execution.ts`, with completion/log/progress finish and outcome mapping behind dedicated ports. |
| Models and installs | `features/models/*`, `app/api/models/discovery/route.ts`, `lib/community/model-discovery.ts`, Agent target sidebar, admin discovery compatibility wrapper | `features/models` | Product `/models` owns the route shell, discovery/install panel, foreground model discovery API, and model-discovery application wrapper; Admin renders only a compact governance handoff, while compatibility exports can be removed after downstream imports disappear. |
| Retrieval | `features/retrieval/RetrievalStudioShell.tsx`, `features/retrieval/contracts.ts`, `features/retrieval/actions.ts`, `features/retrieval/application.ts`, `features/retrieval/query-application.ts`, `lib/agent/retrieval-*`, admin compatibility routes | `features/retrieval` | Foreground route, UI state/actions, CRUD/query handlers, and timeline production are feature-owned; Admin contains only a handoff. |
| Providers | `features/providers/connection-application.ts`, `features/providers/evidence-application.ts`, `features/providers/provider-ops-evidence.ts`, `features/providers/release-probe.ts`, `features/providers/evidence-snapshot-store.ts`, `features/providers/ProviderOpsAdminShell.tsx`, `features/providers/ProviderOpsEvidencePanel.tsx`, `features/providers/AdminProviderComparisonPanel.tsx`, `lib/agent/providers.ts`, `provider-health-desk.ts`, remote policies | `features/providers` | Connection-check and evidence routes are thin; release probes, durable snapshots, pin/retention/export policy, Admin shell/target/comparison rendering are feature-owned. Provider snapshots never extend the 24-hour freshness window beyond their underlying successful event. |
| Runtime | `lib/agent/local-gateway.ts`, `runtime-*`, runtime API routes | `features/runtime` | Runtime risk and process control should be reusable across product/admin. |
| Experiments/timeline | `features/experiments/ExperimentsStudioShell.tsx`, `features/experiments/{V171V190SourceTrainPanel,V1102V1200SourceTrainPanel}.tsx`, `features/experiments/{v171-v190-source-train,v1102-v1200-source-train}.ts`, `features/experiments/ExperimentTimelinePanel.tsx`, `features/experiments/GaReleaseEvidenceBundlePanel.tsx`, `features/experiments/RouteSmokeEvidencePanel.tsx`, `features/experiments/ReleaseSecurityEvidencePanel.tsx`, `features/experiments/ga-release-evidence-bundle.ts`, `features/experiments/ga-release-evidence-application.ts`, `features/experiments/release-security-evidence.ts`, `features/experiments/route-smoke-evidence.ts`, `features/experiments/contracts.ts`, `features/experiments/source-context.ts`, `features/experiments/timeline-service.ts`, `features/experiments/retention-service.ts`, `features/experiments/navigation.ts`, `features/experiments/application.ts`, `lib/agent/timeline-store.ts` | `features/experiments` | Foreground timeline, retention, artifact navigation, checksummed route/security history, source-train projections, live-versus-persisted GA drift, release evidence bundle composition, and producer contracts are feature-owned; structured artifacts and cross-links preserve Fine-tune -> Compare -> Benchmark lineage while JSONL remains infrastructure-only. |
| Admin | `components/admin/*`, admin routes | `features/admin` | Admin composes monitoring/config/audit views only. |

## First Extraction Order

0. P0 inserted before remaining refactor work: align `/compare`, `/models`, `/benchmarks`, `/admin` mirrors, and future templates to the current Agent + Fine-tune studio/workbench design standard before continuing deeper ownership moves.
1. Fine-tune contracts and service seams.
2. Compare contracts and service seams.
3. Fine-tune UI split behind current `/admin` route.
4. Compare UI split behind current `/agent` route.
5. Foreground `/fine-tune` and `/compare` route shells. ✅ `/fine-tune` no longer depends on `AdminFineTunePanel`; `/compare` no longer depends on `AgentWorkbench` and now composes its state/actions through `CompareRouteWorkbench`.
6. Models and Benchmarks foreground route shells. ✅ Initial `/models` and `/benchmarks` shells added with feature contracts.
7. Admin cleanup and route deprecation notes. ✅ Product mirrors are foreground handoffs; runtime operations and provider/telemetry projection have feature ports; old Fine-tune/Retrieval/Models/Timeline Admin APIs advertise a dated compatibility window.

## Guardrails Before Moving Code

- Treat current `/agent` and `/fine-tune` as the canonical visual/interaction source of truth for foreground templates. Route shells and mirrors should converge through `components/layout/StudioPageShell.tsx` before more architecture-only cleanup preserves older template language.
- Do not move persistence and UI in the same patch unless the contract already exists.
- Do not change URLs and file boundaries in the same patch unless route smoke is updated.
- Do not remove current `/admin` entry points until foreground routes pass route and screenshot smoke.
- Every feature crossing should use a contract type, application service, timeline event, or artifact reference.

## Phase 2 / Phase 3 In-Progress Notes

- `lib/finetune/store.ts` is now a compatibility facade over split services; direct API imports should prefer the owning service file.
- `evaluation-service`, `chat-adapter-service`, `export-service`, and `distillation-service` own their operation-specific artifact writes and timeline events.
- `/fine-tune`, `/compare`, `/models`, and `/benchmarks` are foreground product entries. `/admin` now hands benchmark execution to `/benchmarks` and retains provider health, release evidence, and history monitoring; model install management and remaining compatibility mirrors still need final narrowing.
- `AgentCompareSourceSurface` and `AgentFineTuneSourceSurface` now live in `lib/agent/types.ts`; feature contracts alias those types so route ownership and API requests share the same vocabulary.
- Agent session sorting, normalization, merge, export envelope, transcript/session Markdown serialization, title generation, and chat-history flattening now live in `features/agent/session-model.ts`; localStorage persistence, server snapshot GET/PUT, conflict payload normalization, and snapshot merge helpers now live in `features/agent/session-persistence.ts`; stored workbench preference normalization/building now lives in `features/agent/workbench-preferences.ts`; session/preference apply rules live in `features/agent/session-apply.ts`; hydration/server reload/runtime switch history lives in `features/agent/session-hydration.ts`; active session autosave and server sync live in `features/agent/session-server-sync.ts`; session sidebar selectors live in `features/agent/session-sidebar-selectors.ts`; get-code/runtime-rail/session sidebar state starts in `features/agent/workbench-shell-state.ts`; runtime rail state lives in `features/agent/runtime-shell-state.ts`; runtime status/prewarm/action handlers live in `features/agent/runtime-actions.ts`; runtime/target formatters live in `features/agent/runtime-formatters.ts`; target catalog rendering lives in `features/agent/target-catalog-panel.tsx`; runtime/status rail rendering lives in `features/agent/runtime-status-rail.tsx`; transcript panel/container rendering lives in `features/agent/agent-transcript-panel.tsx`; complete transcript turn rendering lives in `features/agent/agent-transcript-turn-card.tsx`; provider self-check rendering lives in `features/agent/agent-provider-self-check-panel.tsx`; connection/scan state lives in `features/agent/connection-shell-state.ts`; connection scan/check handlers live in `features/agent/connection-actions.ts`; transcript scroll/unseen state lives in `features/agent/transcript-shell-state.ts`; transcript follow UI lives in `features/agent/transcript-follow-banner.tsx`; and copy/replay state lives in `features/agent/copy-replay-state.ts`.
- Compare frontend state, workbench implementation, composer/review/lane/recipe components, workbench state model, workbench orchestration model, lifecycle, preference persistence model, reproduce artifacts, review helpers, action calls, recipe persistence, recipe apply/run orchestration, `/compare` route shell, route-owned target sync/preferences, embedded Agent Compare session adapter, embedded workbench adapter, Compare dynamic-loading shell, and workbench prop adapter now route through `features/compare/*`; remaining old Agent Compare files are compatibility re-exports, and remaining Compare work is mostly shrinking generic Agent session/chat coupling around the embedded entry.
- Fine-tune frontend action calls plus surface, setup, run form state, preview builders, submit handlers, clipboard/report actions, chart/report cache, community preset actions/catalog metadata, training args snapshot, tab submit actions, Runs/Assets job actions, and adapter runtime/handoff/proof-loop orchestration now route through `features/finetune/*`; `/fine-tune` now uses `FineTuneStudioPanel` directly, with setup/run/evidence composers and feature-owned prop adapters splitting the foreground composition.
- Route and screenshot smoke now include `/compare`, `/fine-tune`, `/models`, and `/benchmarks` so product IA movement has regression coverage before admin entry points are narrowed further.
