# v1.7.1–v1.9.0 Source-Train Contract Evidence

Date: 2026-08-21

## Delivered source boundary

- Added `/api/experiments/v171-v190-source-train` and an `/experiments` panel.
- Added a typed ten-version projection for v1.7.1 Enterprise Data Plane through
  v1.9.0 Desktop Distribution GA.
- Extended OpenTelemetry wrapping to Benchmark run execution and Fine-tune
  evaluation/distillation operations.
- Added the route to CI route smoke and the full smoke script.

## Truth boundary

The projection's `sourceStatus: pass` only means the repository-owned contract
is implemented and represented by the typed gate. It is not a deployment,
release, production, or external-acceptance result. Every version retains
`externalStatus: hold` and `productionStatus: hold`.

Remaining evidence includes configured pgvector/embedding/reranker and trace
collector delivery; public API load/quota/rate evidence; real artifact transfer;
multi-user identity and review traffic; Apple Developer ID signing,
notarization/staple/Gatekeeper; and independent clean-machine acceptance.

## Validation status

- The Data Analytics competitive report artifact passed schema validation.
- Targeted TypeScript/test execution could not complete in this workspace: the
  Node loader and partitioned typecheck stayed blocked in macOS iCloud
  on-demand file hydration before test assertions executed. The stalled
  processes were stopped. This is an environment limitation, not a passing test
  result; no full app or HTTP smoke success is claimed.
