# v1.1.1 Community/DX Evidence

Date: 2026-07-16

## Scope

This slice absorbs the remaining actionable GitHub issues into `v1.1.1 Model Hub Lifecycle + Community DX`. It improves issue-ready runtime, tool, benchmark, CI, contributor, repository, comparison, and demo evidence without claiming that the authenticated Hub transfer or physical external-disk gate has passed.

## Issue Closure Matrix

| Issue | Delivered evidence | Local status | Final GitHub status |
| --- | --- | --- | --- |
| `#2` Runtime recovery visibility | Admin runtime cards show the latest action, timestamp, phase, and benchmark run ID for the matching target. | Verified by app typecheck and route smoke. | Closed. |
| `#3` Line-level file evidence | `read_file` returns `path:start-end`; Agent tool review renders a numbered evidence excerpt; the system prompt requests minimal line citations. | Verified by app/Agent partitions and running route compilation. | Closed. |
| `#4` CI route smoke | GitHub Actions builds, starts the production app, runs six route/API checks, and uploads JSON plus server log artifacts. | Local route script passed `6/6`; [remote CI run 29495123478](https://github.com/ChrisChen667788/Your-First-LLM-Studio/actions/runs/29495123478) passed every step in `1m18s`. | Closed. |
| `#7` Benchmark issue summary | Every benchmark history card links an exact-run Markdown export with profile, context, target metrics, and failure classes. | Real stored run `bench-readme-smoke-20260628004502` exported `1/1` successful sample at 4096 context. | Closed. |
| `#10` Chinese contributor quickstart | `docs/chinese-contributor-quickstart.md` covers setup, routes, optional MLX, validation, evidence honesty, and PR flow. | Documentation linked from both READMEs and `CONTRIBUTING.md`. | Closed. |
| `#11` Demo recording workflow | Playwright-to-H.264 capture, optional GIF, manifest, MP4, and SHA-256 metadata receipt. | `1280x800`, `15.92s`, `1,347,207` bytes, SHA-256 `50ea5c4f90c71b2afc5dbf652ba7e4adaf9cc7fe84f3a8737aee009fb3cdbe11`; extracted frame inspected without error overlay or secret data. | Closed. |
| `#12` Repository setup checklist | One checklist covers About/topics, community files, branch/CI protection, release evidence, secrets, and final verification. | Documentation linked from the launch kit and READMEs; repository settings remain explicit manual checkboxes. | Closed. |
| `#13` Local/remote lane matrix | Conservative context guidance, task fit, prerequisites, tradeoffs, and skip rules for all current target families. | Documentation linked from both READMEs. | Closed. |

## Validation

- `npm run typecheck:app`: passed.
- `npm run typecheck:changed`: passed all 11 affected partitions.
- `npm run lint`: passed source hygiene for 296 files.
- `npm run build`: production build completed with all application and API routes.
- `npm run smoke:routes`: passed foreground routes and application contracts against the production server, including the benchmark issue-summary endpoint.
- `npm run smoke:ci-routes`: `6/6` passed against the production server for `/`, `/agent`, `/benchmarks`, `/admin`, `/release`, and `/api/experiments/release-train`.
- `npm run validate:screenshots`: `11/11` passed with zero integrity issues.
- `npm run video:release -- --flow agent-workbench`: produced the public MP4 and digest receipt.

## Honest Remaining Gates

- `v1.1.1` Model Hub production evidence still needs a real authenticated multi-file Hub transfer, physical external-disk migration, and checksum/provenance receipt.
- `v1.1.0` GA still needs real Developer ID notarization and an independent clean-machine organization receipt.
