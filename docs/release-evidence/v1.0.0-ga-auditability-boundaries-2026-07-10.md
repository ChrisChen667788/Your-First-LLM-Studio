# v1.0.0 GA Auditability And Boundaries

Date: 2026-07-10

## Delivered

- Release security preflight now writes SHA-256 protected history and supports bounded retention.
- Route smoke reports now carry SHA-256 integrity, failure details, history integrity totals, and JSON/Markdown export.
- GA evidence now distinguishes persisted-file integrity from live-state drift and exposes per-source digests, metrics, and evidence links.
- Compatibility deletion sign-offs support JSON/Markdown export while remaining gated by the 2026-09-30 sunset.
- Provider Ops Admin rendering now enters through `features/providers/ProviderOpsAdminShell.tsx`.
- Provider evidence, GA evidence, and Admin compatibility routes delegate business behavior to feature application services.
- Agent runtime and connection actions now enter the workbench through one feature-owned composition object.

## Runtime Evidence

- Release security: pass; 535 candidate files scanned; 0 secret findings; 0 production dependency vulnerabilities; every new history artifact verified.
- Route smoke: 38/38 passed; 9 UI routes, 25 API contracts, and 4 compatibility-header contracts; current SHA-256 verified; 3/6 history artifacts use the verified format and 3 older artifacts remain explicitly unverified.
- GA drift rehearsal: `drifted` before writing because `route-smoke` changed, then `in-sync` after writing; persisted integrity verified.
- Route smoke JSON/Markdown and compatibility sign-off JSON/Markdown exports returned HTTP 200.
- No paid Provider probe or cloud production rehearsal was executed.

## Remaining External Gates

- A fresh successful remote Provider request or release probe is still required.
- Compatibility deletion sign-off remains unavailable until the sunset and zero-hit gates are ready.
- AWS workload identity, KMS, Object Lock archive, and tied failover evidence remain required for production sign-off.
