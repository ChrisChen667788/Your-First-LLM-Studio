# v3.0.0-v3.1.4 Remediation Control and Service Readiness Source Gate

Date: 2026-08-30

## Verdict

- Source implementation: `PASS`
- Local owner-signal readiness: `ATTENTION`
- Signed external evidence: `0/15`, `HOLD`
- Distribution: `HOLD`
- Production: `BLOCKED`

The 15-version source scope is implemented and locally validated. This gate
does not claim that open remediation, managed infrastructure, representative
workloads, organization approval, or independent review is complete.

## Source Summary

| Projection | Current result |
| --- | --- |
| Version source signals | `5 pass`, `8 attention`, `0 unavailable`, `2 external-only` |
| Control-plane items | `2 satisfied`, `3 open`, `8 blocked`, `2 external-only` |
| Critical attention | `5` |
| External records | `0/15 verified` |
| Production authority | `BLOCKED` |

The eight attention signals consist of the seven existing owner gaps plus the
customer service-readiness disclosure, which now correctly inherits upstream
remediation status instead of reporting a misleading local pass.

## Delivered Contracts

- Fifteen remediation policies with owner, priority, review window,
  dependencies, acceptance checks, next actions, and evidence URI.
- Deterministic acyclic dependency graph and topological order.
- Per-control evidence fingerprint and whole-control-plane state digest.
- Explicit `satisfied | open | blocked | external-only` classification with
  dependency blocker propagation.
- `v3.0.0-v3.0.9` predecessor-bound remediation control train.
- `v3.1.0-v3.1.4` predecessor-bound service-readiness train.
- Thin read-only APIs, Experiments UI, machine-readable export, bilingual docs,
  and route ownership contracts.
- 140-entry release train and evidence matrix alignment.

## Local Validation Evidence

- Tests: `125/125 PASS`.
- Full TypeScript check: `PASS`.
- Lint/source hygiene: `954 files`, `PASS`.
- Production build: `PASS`, including both new dynamic API routes.
- Architecture budget: `6/6 targets met`, `0 debt lines`.
- Durable-state boundary validation: `PASS`, `0 unknown stores`.
- Release-truth validation: `PASS`; source `v1.5.1`, distribution `HOLD`,
  production `BLOCKED`.
- Security preflight: `1,239 candidate files`, `0 secret findings`,
  `0 production vulnerabilities`.
- CI route smoke: `75/75 PASS`.
- Full cross-surface route smoke: `PASS`, including both new trains, the
  140-entry release train, and the 140-round evidence matrix.
- Browser QA: no console warnings/errors; desktop `1280x720` and mobile
  `390x844` have no horizontal overflow; train switching renders `10` and `5`
  cards respectively.
- Export:
  `output/release-evidence/v3.0.0-v3.1.4-remediation-service-readiness.json`.

## Remaining Owner Actions

1. Record representative Provider traffic and a successful release probe.
2. Configure and exercise managed Retrieval dependencies and frozen probes.
3. Record an authenticated immutable multi-file Hub transfer.
4. Record signed workspace action provenance and reconcile database audit.
5. Add comparable runtime performance and six-operation recovery evidence.
6. Run a distinct complete 500-item Benchmark candidate.
7. Configure a real OTLP/Langfuse exporter and reconcile an end-to-end trace.

Independent remediation acceptance, service disclosure approval, support
incident replay, clean-machine upgrade review, accountable-owner transition,
immutable archive, distribution, and production authorization remain external
gates. Repository code cannot complete or bypass them.
