# v3.2.0-v3.3.4 Source Gate: Remediation Execution and Operational Acceptance

Date: 2026-08-30

## Result

Repository source gate: `PASS`

Local operational state: `ATTENTION`

External evidence: `HOLD` (`0/15` independently verified records)

Distribution: `HOLD`

Production: `BLOCKED`

The source gate passes because the planned contracts, read models, APIs, UI,
tests, build, export, and security checks are complete. The local operational
state remains attention because the seven owner actions are 0 satisfied, 3
ready, and 4 blocked. This document does not approve those actions, a
distribution, or production.

## Implemented source

- Seven remediation execution actions joined directly to the existing owner
  control plane.
- Deterministic SHA-256 idempotency keys, 900-second leases, fencing-token
  digests, stale-writer denial, rollback instructions, and evidence
  fingerprints.
- Read-only v3.2.0-v3.2.9 execution assurance train.
- Read-only v3.3.0-v3.3.4 operational acceptance train.
- Thin `/api/experiments/remediation-execution` and
  `/api/experiments/operational-acceptance` wrappers.
- Four-stage Experiments closure panel covering v3.0.0-v3.3.4.
- Machine-readable evidence export and route ownership entries.
- 15 new typed release-train milestones; total milestone count is 155.

## Validation

| Gate | Result |
| --- | --- |
| Unit and contract tests | 131/131 pass |
| Full TypeScript check | pass |
| Lint and source hygiene | pass, 960 source files |
| Architecture budgets | pass, 6/6 targets, 0 debt lines |
| Production build | pass, 53 static pages generated |
| CI route smoke | 77/77 pass |
| Full cross-surface route smoke | pass |
| Release-security preflight | pass, 1253 candidate files, 0 findings, 0 production vulnerabilities |
| Desktop browser QA | pass at 1232px content width, no console errors or horizontal overflow |
| Mobile browser QA | pass at 390x844, no console errors or horizontal overflow |
| Evidence export | pass |

Evidence export:
`output/release-evidence/v3.2.0-v3.3.4-remediation-execution-operational-acceptance.json`

SHA-256:
`649d09f569abb04723c0637b215029593e32048c0bf459709de3f406472dbf25`

## Source projection

Across the 15 new versions:

- 4 source signals pass.
- 9 source signals need attention.
- 0 source signals are unavailable.
- 2 source signals are external-only.
- 0 of 15 external assurance records verify.

The v3.2 train reads 2 pass, 7 attention, and 1 external-only signal. The v3.3
train reads 2 pass, 2 attention, and 1 external-only signal. All external
records are missing, every external chain is incomplete, and every version
keeps `localProductionTransitionDenied=true`.

## Remaining owner work

1. Run representative provider traffic and retain release-probe reconciliation.
2. Configure managed Retrieval dependencies and run the frozen probe set.
3. Perform an authenticated immutable multi-file Hub transfer and read-back.
4. Execute a trusted signed workspace action and reconcile database audit.
5. Produce comparable runtime receipts and complete six-operation recovery.
6. Run a distinct complete qualified Benchmark candidate.
7. Export a real OTLP or Langfuse trace and reconcile usage and cost.
8. Import only independently signed, pinned, fresh, predecessor-bound evidence.

Local fixtures, source code, and this gate cannot substitute for those actions.
