# v2.1.0–v2.1.9 Post-GA Operations — source-gate evidence

Date: 2026-08-22
Decision: all ten repository-owned source contracts are present; external and
production authority remain `HOLD` / `BLOCKED`.

## Implemented surfaces

- Read-only verifier: `features/experiments/post-ga-operations-train.ts`
- API: `GET /api/experiments/post-ga-operations-train`
- Experiments panel: `PostGaOperationsTrainPanel`
- Regression tests: `tests/post-ga-operations-train.test.ts`
- Evidence export: `npm run evidence:v210-v219-post-ga-operations`

## Verified properties

1. `v2.1.0` starts only from a verified `v2.0.5` closure archive digest.
2. Each subsequent record binds the prior verified version, digest, and record id.
3. Every record requires a valid SHA-256 digest, pinned RSA key, detached
   signature, durable issuer, valid freshness window, and version-specific
   control assertions.
4. The terminal `v2.1.9` review requires the ordered full digest chain and a
   reviewer distinct from every operational record issuer.
5. The response is permanently fail-closed: `sourceStatus` is source-contract
   truth only; `externalStatus` stays `hold` and `productionStatus` stays
   `blocked`, including with a fully valid synthetic chain.

No record minting, signing, deployment, failover, deletion, billing settlement,
or production authorization occurs in the repository.
