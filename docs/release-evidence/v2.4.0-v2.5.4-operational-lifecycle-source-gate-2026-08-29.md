# v2.4.0-v2.5.4 Operational Lifecycle Source Gate

Date: 2026-08-29

## Decision

- Source implementation: `PASS`
- Local operational projection: `ATTENTION`
- External assurance chain: `HOLD` (`0/15` independently verified)
- Distribution: `HOLD`
- Production: `BLOCKED`

This record proves the source contracts, local read models, APIs, UI projection,
export path, and regression gates for v2.4.0-v2.5.4. It does not claim that an
external operator reviewed the records or that production was authorized.

## Delivered Scope

- v2.4.0-v2.4.9 adds feature-owned source signals for runtime fleet,
  provider reliability, workload SLO, token/cost reconciliation, benchmark and
  retrieval drift, Agent action safety, Workflow recovery, Fine-tune ROI, and
  independent operations review.
- v2.5.0-v2.5.4 adds deployment portability, data sovereignty,
  customer-controlled key projection, continuity/exit rehearsal, and an
  independent lifecycle closure.
- Experiments now exposes both trains through thin application routes and a
  single operational lifecycle panel.
- External evidence remains detached-signature, pinned-key, digest,
  freshness, predecessor, semantic, and reviewer-independence verified.

## Local Signal Result

| Result | Count | Meaning |
| --- | ---: | --- |
| Pass | 8 | A usable local source signal is present. |
| Attention | 5 | Source integration exists, but current local evidence is incomplete or sparse. |
| External-only | 2 | Only an independent signed review can satisfy the milestone. |

The five attention signals are expected and visible: no recent provider
requests, limited SLO traffic, no provider-token sample, one retrieval replay
record, and enterprise Retrieval dependencies not configured. None are promoted
to pass by fixtures or source completeness.

## Verification

- `npm test`: 108/108 passed.
- `npm run typecheck:changed`: all 12 affected partitions passed.
- `npm run lint`: source hygiene passed for 932 files.
- `npm run build`: Next.js 15.5.21 production build passed; 53 static pages generated.
- `npm run smoke:ci-routes`: 69/69 checks passed.
- `npm run smoke:routes`: full UI and API contract smoke passed.
- `npm run preflight:release-security`: 1,208 candidate files, 0 findings,
  0 production vulnerabilities; integrity verified and fresh in the live API.
- `npm run validate:architecture-budgets`: 0 ceiling breaches and 0 remaining debt lines.
- `npm run validate:durable-state`: 12 boundaries scanned, 0 unknown and 0 stale exemptions.
- `npm run validate:release-truth`: source v1.5.1 complete/local pass,
  distribution hold, production blocked.
- `npm run validate:finetune-evidence`: workflow evidence pass; quality promotion hold.

## Stability Repairs

- Production evidence and release-decision readers now accept an injectable
  clock, keeping freshness checks strict while making signature regressions
  deterministic across calendar boundaries.
- Release-security evidence resolves from `FIRST_LLM_WORKSPACE_ROOT`, so a
  standalone Next.js server reads the same report/history paths as the source
  checkout and retention workflow.
- The working checkout used for validation was migrated from the cloud-managed
  Documents location to `~/Developer/First-LLM-Studio`; the prior checkout was
  repeatedly re-offloaded into macOS dataless files during tests.

## Remaining Gate

All 15 external records are still absent. Independent issuers must supply the
signed, trust-pinned, fresh, predecessor-bound records before the chain can
advance from `HOLD`; this repository cannot mint those records or authorize
production.
