# v1.3.1 Workflow Graph Studio Local Acceptance

Date: 2026-07-23
Local status: **PASS**
Production status: **BLOCKED**

Machine-readable evidence: [`v1.3.1-workflow-studio-acceptance-2026-07-23.json`](./v1.3.1-workflow-studio-acceptance-2026-07-23.json)

## Product Closure

- `/workflows` is now a real visual authoring surface with draggable typed nodes, condition-labelled transitions, node policy editing, persisted layout, breakpoints, and an execution lane.
- Graph versions pin a runtime profile and immutable input-artifact digests.
- Draft writes use optimistic revision preconditions; stale edits fail closed and published versions are immutable.
- Strict validation covers schema/id rules, entry and exit cardinality, reachability, dead ends, duplicate edges, cycles, approval policy, and side-effect resume policy.
- The safe worker understands model and guard branch conditions, stops at approval or protected side-effect boundaries, and resumes with idempotency evidence.
- Published graph versions expose durable invocation plus an OpenAI-compatible `/v1/chat/completions` endpoint and generated curl/JavaScript/Python examples.

## Acceptance Result

The repeatable runner passed **16/16** checks across graph validation, invalid-cycle rejection, typed guard branches, runtime/artifact pins, optimistic conflict rejection, graph digest and immutability, approval boundaries, duplicate side-effect suppression, completion, replay isolation, state diff, deployment-key version authorization/revocation, OpenAI contract acceptance, and version diff.

Two consecutive runs produced different immutable graph-version digests, as expected, but the same stable report digest after excluding timestamps, execution IDs, and generated graph versions:

```text
sha256:1608a1c61137464b2c034653d9d33c7a66eafe2d119371d86e8d87bcbcf864c0
```

The unified post-v1 gate reports:

- `status=externally-blocked`
- `localReady=true`
- `productionReady=false`

## Verification

```bash
npm run typecheck:changed
npm run lint
npm run build
BASE_URL=http://127.0.0.1:3011 npm run evidence:v131-workflows -- --publish
BASE_URL=http://127.0.0.1:3011 npm run smoke:routes
BASE_URL=http://127.0.0.1:3011 npm run screenshots:release -- --flow workflow-graph-studio
npm run validate:screenshots
```

## Production Gates

This local receipt does not claim production promotion. The following remain required:

1. Authenticated non-loopback OpenAI-compatible invocation evidence.
2. Distributed worker lease failover and restart-recovery evidence.
3. Multi-user draft conflict and audit evidence.

## Dependency Audit

The directly remediable `fast-uri` advisory is pinned to `3.1.4`. `npm audit --omit=dev` still reports five transitive findings in `@hono/node-server@1.19.14` through the pinned MCP SDK and `sharp@0.34.5` through Next.js. The suggested forced fix would downgrade the MCP SDK or Next.js across major application boundaries, so these remain explicit upstream dependency blockers rather than being hidden behind a forced lockfile rewrite.

## 中文摘要

`v1.3.1` 已把 `/workflows` 从节点展示页推进为可编辑、可校验、可发布、可断点恢复、可回放并可部署为 API 的工作流工作台。本地真实验收连续两次通过 16/16，稳定 report digest 一致；但认证非回环调用、分布式 worker 恢复和多用户冲突审计仍缺少真实证据，因此生产状态继续保持 `BLOCKED`。
