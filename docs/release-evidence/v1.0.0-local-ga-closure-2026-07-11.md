# v1.0.0 Local GA Closure Evidence

Date: 2026-07-11

## Scope

This batch closes the remaining local v1.0 engineering and evidence tasks:

1. Agent base target, session, chat, protected-tool decision, and sync state creation moved to `features/agent/chat-session-state.ts`.
2. Benchmark execution groups and result metric cards moved to `features/benchmark/AdminBenchmarkResultGroups.tsx`.
3. Admin recent chats, latency percentiles, model/context breakdowns, connection checks, and observability paths moved to `features/admin/AdminRecentOperationsPanel.tsx`.
4. Local runtime timeout, probe, auto-ensure, and recovering policy moved to `features/agent/runtime-local-health.ts`.
5. Admin compatibility deletion manifest now separates pre-sunset readiness from the dated deletion/sign-off gate.
6. The compatibility pre-sunset rehearsal is 5/5 ready: canonical replacements exist, header smoke coverage is present, runtime hits are zero, and historical unclassified hits are zero.
7. Demo capture now covers Agent, Compare, Models, Benchmarks, Retrieval, Admin, Release, Experiments, and Fine-tune at 1920x1200 with 2x DPR.
8. Studio/Admin captures use fixed-viewport screenshots; long evidence pages use full-page capture, preventing blurry oversized workbench images and blank scroll regions.
9. A real DeepSeek release probe passed both the model-list and minimal chat round-trip stages and produced a pinned qualifying Provider evidence snapshot.

## Real Provider Evidence

- Target: `deepseek-api`.
- Resolved model: `deepseek-reasoner`.
- Models stage: HTTP 200, 2785 ms.
- Chat stage: HTTP 200, 196 ms, sample response `CHAT_OK`.
- Release probes in the current 24-hour window: 1 success, 0 failures.
- Provider evidence snapshot: qualifying and pinned.
- No API key was copied into this checkout or written to release evidence.

## Verification

- Focused `agent-ui`, `agent-api`, `admin`, and `app` typechecks: PASS.
- Nine-flow high-resolution screenshot capture: PASS.
- Visual QA for Agent and Admin fixed-viewport captures: PASS.
- `npm run typecheck:changed`: PASS, 11/11 partitions.
- Cold-start route smoke: PASS, 41/41 checks.
- UI routes: 9/9; API contracts: 28/28; compatibility headers: 4/4.
- Route-smoke integrity: SHA-256 verified; 14 archived reports, 13 passes, 1 preserved failure, and 6 consecutive current passes.
- Release security preflight: PASS, 573 candidate files, zero secret findings, and zero production dependency vulnerabilities.
- Public release evidence: 100%, 9/9 demo flows verified, no blockers.
- Promotion gate: PASS after the real Provider release probe.
- GA bundle: 7 sources, 16 archived bundles, persisted verification `in-sync` with zero changed source ids.
- v1.0 release matrix: 100% local check completion, status `evidence-needed` because five external/time-bound blockers remain.
- Non-cloud readiness: 83%, with only the compatibility sunset date outstanding.
- Production readiness: 71%, blocked by the sunset date plus real cloud adapter, Object Lock, KMS, and tied failover evidence.

## External Gates

Local GA closure does not authorize removal of compatibility wrappers before `2026-09-30`. It also does not substitute local control-plane evidence for real workload identity, cloud KMS/HSM, immutable object storage, or tied production failover evidence. Those gates remain fail-closed.
