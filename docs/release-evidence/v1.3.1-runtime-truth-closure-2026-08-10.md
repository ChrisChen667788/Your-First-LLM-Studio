# v1.3.1 Runtime Truth Closure

Date: 2026-08-10

## Repository-closable work completed

- DeepSeek now defaults to the account-advertised `deepseek-v4-pro`, probes `/models`, and can downgrade to `deepseek-v4-flash`.
- Public `/v1/chat/completions` delegates to the shared Provider completion port and no longer returns a fixed synthetic completion.
- Fine-tune adapter chat and evaluation require an attached runtime and execute real adapter inference. Distillation executes the selected teacher target and records skipped provider failures.
- Workflow model nodes execute the Provider port. The protected-tool/resume shadow uses the installed LangGraph runtime, an interrupt, a checkpointer, a resume command, and durable duplicate-side-effect evidence.
- Enterprise Retrieval has executable pgvector migration/index/query ports, OpenAI-compatible embeddings, cross-encoder reranking, citations, workspace/subject/group ACL filters, and PostgreSQL RLS. It fails closed while services are unconfigured.
- Shared OpenTelemetry export supports generic OTLP/HTTP or Langfuse OTLP ingestion, uses bounded batching plus shutdown flushing, and records local span receipts without prompt or secret payloads.

## Real local runtime evidence

- Public `/v1/chat/completions` executed `local-qwen3-0.6b` and returned 88 prompt, 30 completion, and 118 total tokens.
- A paid DeepSeek request probed both account-advertised V4 models, attempted V4 Pro, automatically recovered on V4 Flash, returned `DEEPSEEK_V4_OK`, and recorded 139 total tokens.
- Workflow safe worker executed a real model node and stopped at `waiting-approval`; the model event produced a protected-tool plan.
- LangGraph receipt `langgraph-shadow-receipt-7e476daa-e0bd-43d3-b8f0-d26046e15667` interrupted and resumed once, executed one protected action, and recorded zero duplicate side effects.
- Adapter chat operation `ft-op-chat-2b55362c-e10f-472c-841a-50e5e493d7fe` completed through the mounted MLX adapter with 110 total tokens.
- Adapter evaluation `ft-op-eval-56f5f72c-37fa-4126-995f-c182c5559bc5` retained 1/1 real prediction with token-overlap F1 `0.5714`.
- Distillation operation `ft-op-distill-05061198-1a4c-4e90-9e59-6f6c0607b6f8` retained and validated 1/1 real teacher row.
- A loopback OTLP/HTTP collector received a 4,322-byte `/v1/traces` export batch after an immediate forced flush. This proves the batched exporter contract locally; it is not a Langfuse or production collector receipt.

## Release truth retained

Source remains `1.3.1`. Distribution remains `HOLD`; production remains `BLOCKED`.

The repository now contains strict adapters and acceptance read models, but this machine does not have organization-controlled OIDC/SCIM, deployed PostgreSQL RLS, Enterprise Retrieval services, an OTLP/Langfuse project, AWS workload identity, a KMS key, or an Object Lock bucket. Local contracts are not production receipts.

Runtime status: `/api/experiments/external-production-readiness`.

Validation at this checkpoint: 41/41 tests passed, all changed TypeScript partitions passed, the full dependency audit reported zero known vulnerabilities, and real local/remote inference evidence was retained above.
