# v1.6 Feature Ownership Evidence

Date: 2026-08-05

## Scope

This local batch covers fifteen ownership checks across Fine-tune physical composition, canonical Benchmark and Compare routes, compatibility wrappers, architecture budgets, and Compare foreground mode isolation.

## Implemented Boundaries

- `/fine-tune` imports `features/finetune/FineTuneStudioShell.tsx` directly.
- Fine-tune shell, composition panel, and setup/run/evidence composers are physically owned by `features/finetune`.
- Fine-tune, Compare, and Benchmark Studio use `/api/benchmarks` for run submission.
- Compare progress reads and writes use `/api/compare/progress`.
- `/api/agent/compare/progress` is retained as a thin compatibility re-export.
- The foreground Compare route no longer creates `AgentWorkbenchMode` state.
- `/experiments` exposes the durable `experiments.v16-feature-ownership.v1` read-model.

## Verification

- Durable feature ownership contract: 15/15 local checks pass with SHA-256 `166922e74c753b5f36461cc8fc7b029ee8dd0f1846cc110db2eb88ba677923c2`.
- Full test suite: 28/28 pass, including the new legacy Agent session migration regression.
- Changed TypeScript partitions: 11/11 pass; `core-agent` and `app` were rerun after the session migration fix.
- Source hygiene: 747 files pass.
- Architecture budgets: 6/6 tracked targets pass with zero remaining debt lines.
- Cross-surface route smoke: 141/141 pass across 10 UI routes, 127 API contracts, and 4 compatibility-header contracts.
- Route smoke SHA-256: `04ace8f4a409900cc8d8e3461cb9cc49e7912d16745babe090d470d3aa5538cc`.
- A legacy `0.2.1` Agent session snapshot was recovered and migrated in place to `0.3.0`; the quarantined source copy remains available for audit.
- Production status remains `HOLD`; this evidence does not represent cloud or organization sign-off.

## Remaining External Gates

- Trusted external IdP and SCIM lifecycle evidence.
- Remote durable lease and independent worker failover evidence.
- Cloud KMS/HSM and immutable archive receipts.
- Managed multi-node production rehearsal and organization acceptance.
