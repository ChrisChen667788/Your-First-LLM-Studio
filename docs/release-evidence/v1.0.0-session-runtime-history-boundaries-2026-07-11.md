# v1.0.0 Session, Runtime, and History Boundary Evidence

Date: 2026-07-11

## Scope

This non-cloud GA continuation completed eight boundary slices:

1. Moved Agent session create, rename, pin, delete, and bulk-clear actions into `features/agent/session-command-actions.ts`.
2. Moved session export envelope and browser download behavior into `features/agent/session-export.ts`.
3. Added `features/agent/turn-lifecycle-input.ts` so chat submit, replay, resume, and protected-tool decision dependencies enter one typed lifecycle input.
4. Moved Admin runtime trace rendering into `features/admin/AdminRuntimeTracePanel.tsx`.
5. Moved Admin runtime model-state and refresh/prewarm/release/restart/log actions into `features/admin/AdminRuntimeModelStatePanel.tsx`.
6. Moved Benchmark history identity, execution/failure summary, evidence source, and pin/report actions into `features/benchmark/AdminBenchmarkHistoryEntryHeader.tsx`.
7. Moved Compare handoff run-note detail and copy state into `features/benchmark/AdminBenchmarkRunNotePanel.tsx`.
8. Moved local runtime recovering, ready, and unavailable payload assembly into `features/agent/runtime-local-status.ts`.

## Boundary Result

- `AgentWorkbench.tsx` is 1,979 lines after this slice and no longer constructs session command/export implementations inline.
- `AdminDashboard.tsx` is 3,172 lines after this slice and no longer owns runtime trace/model action or Benchmark history header/run-note rendering.
- `runtime-status-application.ts` is 200 lines and now focuses on request validation, target resolution, gateway probing/auto-start, and metric collection.
- Local and remote runtime response construction are separate feature-owned builders.

## Verification

- Focused `agent-ui`, `agent-api`, and `admin` typechecks: PASS.
- `npm run typecheck:changed`: PASS, 11/11 partitions.
- Cold-start route smoke: PASS, 41/41 checks.
- UI routes: 9/9.
- API contracts: 28/28.
- Compatibility headers: 4/4.
- Route-smoke integrity: SHA-256 verified.
- Route-smoke history: 13 reports, 12 passes, 1 preserved failure, and 5 consecutive current passes.
- Release security preflight: PASS, 568 candidate files, zero secret findings, and zero production dependency vulnerabilities.
- GA evidence bundle: written and verified `in-sync`, with 7 sources and 15 archived bundles.
- v1.0 release matrix: 85%, `evidence-needed`, with 6 external/evidence blockers.

## Gate Truth

This refactor does not satisfy remote Provider freshness, the 2026-09-30 compatibility sunset, AWS workload identity, KMS, Object Lock, or tied failover evidence. No paid remote request or cloud production rehearsal was run for this batch.
