# Post-v1 15-slice closure evidence

Date: 2026-07-14

## Decision

The local post-v1 closure rehearsal currently reports **12 ready, 0 partial, and 3 blocked** slices, with **84% average completion**. The blocked slices are intentionally fail-closed because this workstation has no Apple Developer ID/notary profile, OIDC workload configuration, or SCIM bearer secret. Local rehearsal evidence is not presented as external production sign-off.

## Implemented slices

| # | Version | Slice | Result | Durable evidence |
| --- | --- | --- | --- | --- |
| 1 | v1.1.0 | Developer ID release pipeline | Blocked externally | `/api/desktop/apple-release-signing` |
| 2 | v1.1.1 | Hub retry and bounded backoff | Ready | `/api/models/hub-transfers` |
| 3 | v1.1.1 | Content-address reconciliation | Ready | `/api/models/content-index` |
| 4 | v1.2.0 | Local Server safety policy | Ready | `/api/models/server-instances` |
| 5 | v1.2.0 | Sanitized request and token ledger | Ready | `/api/models/server-instances/requests` |
| 6 | v1.2.1 | OpenAI-compatible model conformance | Ready | `/api/runtime/openai-conformance` |
| 7 | v1.3.0 | Dependency-first extension install plan | Ready | `/api/extensions/install-plan` |
| 8 | v1.3.0 | Node permission sandbox policy | Ready | `/api/extensions/sandbox` |
| 9 | v1.3.1 | Immutable workflow graph registry | Ready | `/api/workflows` |
| 10 | v1.3.1 | Workflow deploy-as-API ingress | Ready | `/api/workflows/deploy/protected-tool-resume` |
| 11 | v1.4.0 | OIDC discovery and JWKS validation | Blocked externally | `/api/governance/identity` |
| 12 | v1.4.0 | SCIM user/group lifecycle | Blocked externally | `/api/scim/v2/Users`, `/api/scim/v2/Groups` |
| 13 | v1.4.0 | Transaction-local Postgres RLS context | Ready | `postgres-rls-rehearsal.json` |
| 14 | v1.4.1 | Paired evaluation confidence gate | Ready | `/api/evaluation/statistics` |
| 15 | v1.5.0 | Signed artifact provenance gate | Ready | `/api/artifacts/packages` |

## Real local evidence

- Ollama `qwen3:0.6b` passed live `/v1/models` and `/v1/chat/completions` checks. The latest passing request recorded 21 prompt tokens, 79 completion tokens, 8 ms model-list latency, and 11,298 ms chat latency; latency is retained as observed rather than normalized.
- The Postgres 16 rehearsal applied migrations `002` and `003`, enforced forced RLS, denied cross-workspace access, and confirmed that workspace/actor context clears after the transaction.
- The paired evaluation used 40 samples and produced a mean delta of `0.08195`, above the `0.05` gate, with a 95% interval of `[0.0814345, 0.0824655]`.
- The artifact rehearsal generated an ephemeral Ed25519 key pair and passed server-side digest, signature, dependency, secret-scan, source, builder, SBOM, and evidence checks. This proves the local verifier path only; it is not a cloud KMS receipt.
- Extension installation resolves dependencies before dependents and records required permission approvals. The process sandbox denies network, secret, filesystem-write, and child-process capabilities for the local Node rehearsal.

## Reproduction

```bash
FIRST_LLM_STUDIO_BASE_URL=http://127.0.0.1:3011 npm run rehearse:post-v1-closure
npm run typecheck:changed
npm run smoke:routes
```

The aggregate read-model is available at `/api/experiments/post-v1-closure` and is rendered in `/experiments`.

## Remaining external gates

1. Sign and notarize a desktop package with a real Apple Developer ID and notarization profile, then staple and verify the distributed artifact.
2. Configure a real OIDC issuer/client/audience and capture a signed ID-token verification receipt against discovery metadata and JWKS.
3. Configure a rotated SCIM bearer secret and run create, patch, membership, deactivate, and delete lifecycle evidence against the production identity provider.

Background workflow model/tool execution and destructive content deduplication remain later hardening work. They do not weaken the newly added registry, ingress, reconciliation, or fail-closed policy contracts.
