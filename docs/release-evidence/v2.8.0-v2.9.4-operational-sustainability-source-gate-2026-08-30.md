# v2.8.0-v2.9.4 Operational Sustainability Source Gate

Date: 2026-08-30

## Verdict

- Source implementation: `PASS`
- Local owner-signal readiness: `ATTENTION`
- Signed external evidence: `0/15`, `HOLD`
- Distribution: `HOLD`
- Production: `BLOCKED`

The 15-version source train is implemented and validated. This gate does not
claim that its seven owner-signal gaps or two independent reviews are complete.

## Signal Summary

| Source signal | Status | Current reason |
| --- | --- | --- |
| Provider traffic and fallback reconciliation | `ATTENTION` | No representative traffic or successful release probe in the 168-hour window |
| Retrieval freshness and ACL remediation | `ATTENTION` | Managed pgvector, embedding, reranker, and RLS dependencies are not configured |
| Model supply-chain reconciliation | `ATTENTION` | No authenticated immutable Hub receipt binds the local checksum set |
| Workspace audit completeness | `ATTENTION` | Database audit exists; signed workspace action provenance is missing |
| Runtime recovery and capacity efficiency | `ATTENTION` | No comparable performance pair or complete six-operation recovery set |
| Agent action and session recovery | `PASS` | Protected-action shadow, replay, state diff, and zero-duplicate evidence available |
| Workflow queue and failover efficiency | `PASS` | Local worker and process-isolated failover evidence available |
| Benchmark cost-quality decision gate | `ATTENTION` | One complete 500-item run exists; a distinct candidate run is missing |
| Fine-tune cost-quality and export gate | `PASS` | 15/15 quality/export slices and package read-back available |
| Independent remediation review | `EXTERNAL ONLY` | Local evidence cannot replace an independent reviewer |
| Telemetry and resource transparency | `ATTENTION` | Trace receipts exist; exporter is disabled and runtime comparison is incomplete |
| Incident diagnostics and retention | `PASS` | Connection checks, supervisor receipts, and integrity-checked snapshots available |
| Admin compatibility sunset readiness | `PASS` | All five wrappers satisfy the pre-sunset source threshold |
| Desktop upgrade and data lifecycle | `PASS` | Update rollback, migration/restore, and permission repair receipts available |
| Independent sustainable-operations closure | `EXTERNAL ONLY` | Local evidence cannot replace an independent reviewer |

Totals: `6 pass`, `7 attention`, `0 unavailable`, `2 external-only`.

## Delivered Contracts

- Generic source-backed assurance projection with typed signal ids.
- 15 exception-safe owner projections across Provider, Retrieval, Models,
  Governance, Agent, Workflow, Benchmark, Fine-tune, Telemetry, Admin, and Desktop.
- Strict predecessor-bound v2.8 and v2.9 external chains.
- Thin read-only APIs for both trains.
- Experiments UI with local/signed/production states, source links, refresh, and
  v2.8/v2.9 segmented controls.
- 125-entry release train and evidence matrix alignment.
- Machine-readable evidence export.
- Bilingual GitHub/ModelScope product and roadmap documentation.
- Route ownership and source-gate documentation.

## Validation Evidence

- Tests: `119/119 PASS`.
- Full TypeScript check: `PASS`.
- Lint/source hygiene: `946 files`, `PASS`.
- Production build: `PASS`, including both new dynamic API routes.
- Architecture budget: `6/6 targets met`, `0 debt lines`.
- Durable-state boundary validation: `PASS`, `0 unknown stores`.
- Release-truth validation: `PASS`; source `v1.5.1`, distribution `HOLD`, production `BLOCKED`.
- Security preflight: `1,228 candidate files`, `0 secret findings`, `0 production vulnerabilities`.
- CI route smoke: `73/73 PASS`.
- Full cross-surface route smoke: `PASS`, including both new trains, the
  125-entry release train, and the 125-round evidence matrix.
- Browser QA: fresh production page, no console warnings/errors; desktop
  `1280x720` and mobile `390x844` have no horizontal overflow; train switching
  renders `10` and `5` cards respectively.
- Export: `output/release-evidence/v2.8.0-v2.9.4-operational-sustainability.json`.

## Next Source Actions

1. Record representative Provider traffic and release-probe evidence.
2. Configure and exercise managed Retrieval dependencies.
3. Record an authenticated immutable multi-file Hub transfer.
4. Record signed workspace action provenance.
5. Add comparable runtime performance and six-operation recovery evidence.
6. Run a distinct complete 500-item Benchmark candidate.
7. Enable a real OTLP/Langfuse exporter and bind traces to runtime receipts.

Independent reviewer, managed infrastructure, billing, clean-machine, and
organization evidence remain external gates and cannot be completed by this repository.
