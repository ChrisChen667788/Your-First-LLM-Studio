# v1.3.1 Local Gate Reliability Evidence

Date: 2026-08-01

Status: local repository and Apple Silicon Runtime Fabric gates `PASS`; external production gates remain `HOLD`/`BLOCKED`.

## Reliability Changes

| Gate | Implemented behavior | Verified result |
| --- | --- | --- |
| Changed-file typecheck | Git status discovery stops after `TYPECHECK_GIT_STATUS_TIMEOUT_SECONDS` and conservatively selects all partitions. | A forced one-second timeout selected all 11 partitions and left no child `git status` process. |
| Source hygiene | Scans `app`, `components`, `features`, `lib`, `pages`, and `scripts`; each source read has a bounded timeout and reports its path/error code. | 705 source files passed. |
| Runtime Fabric acceptance | Records attempt duration, status, blockers, and retryable failure state; retries only an explicitly retryable backend failure, with at most three attempts. | The real run passed on attempt 1 in 6,533 ms. |

## Verification

| Command | Result |
| --- | --- |
| `npm run typecheck:partitions` | `PASS`, 11/11 partitions |
| `npm run lint` | `PASS`, 705 source files |
| `npm test` | `PASS`, 19/19 tests |
| `npm run validate:architecture-budgets` | `PASS`, 6/6 targets, zero debt lines |
| `npm run validate:durable-state` | `PASS`, zero unknown or stale exemptions |
| `npm run validate:release-truth` | `PASS`, source milestone `v1.3.1`, fail-closed channels preserved |
| `BASE_URL=http://127.0.0.1:3011 npm run smoke:ci-routes` | `PASS`, 22/22 routes and API contracts |
| `npm audit --omit=dev` | `PASS`, zero vulnerabilities |

## Real Runtime Receipt

- MLX: `local-qwen3-0.6b`
- Ollama: `qwen3:0.6b`
- llama.cpp: `qwen3-0.6b-llamacpp`
- Real backends: 3/3 passing
- Adapter contracts: 6/6 conformant
- Normalized operation checks: 42/42
- Average acceptance latency: 1,064 ms
- Acceptance digest: `6642c7e3fd045dbd5792f7eaf8a7adcc09c32b72d9ce2115312a9146a0a19441`
- Promotion digest: `d1a862e4b1d553f55ff34a31ba609d1696d72055f958498f678a9ea9e0142c5c`
- Machine-readable evidence: [`v1.2.1-runtime-fabric-acceptance-2026-08-01.json`](./v1.2.1-runtime-fabric-acceptance-2026-08-01.json)

## Explicit Holds

- Runtime Fabric production evidence still requires real LocalAI, Linux/NVIDIA vLLM and SGLang, and heterogeneous separate-node failover receipts.
- Desktop distribution still requires Developer ID signing/notarization and an independent clean-machine organization receipt.
- Production governance still requires real IdP/SCIM lifecycle, deployed Postgres concurrency/RLS, authenticated non-loopback use, immutable external archive, and organization sign-off.
- Cloud control-plane promotion still requires workload identity, real KMS/HSM, and immutable object-storage receipts.

No local fixture or single-machine rehearsal is counted as evidence for those gates.
