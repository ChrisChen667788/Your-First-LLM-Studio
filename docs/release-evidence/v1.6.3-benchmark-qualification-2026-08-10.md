# v1.6.3 Benchmark Qualification Evidence

Date: 2026-08-10

## Decision

- Local qualification: `PASS` (`15/15`).
- Qualified workload: `math-500-qualified`.
- Official leaderboard score: `HOLD`.
- Distribution: `HOLD`.
- Production: `BLOCKED` under the active `1.5.1` release truth.

This checkpoint qualifies one real official benchmark asset without presenting a compatibility run as an official score. The upstream MATH-500 `test.jsonl` payload was fetched from an immutable Hugging Face dataset revision, persisted locally, hashed, parsed, and reverified before it became selectable in Benchmark Studio.

## Pinned Artifact

| Field | Value |
| --- | --- |
| Repository | `HuggingFaceH4/MATH-500` |
| Split / file | `test` / `test.jsonl` |
| Revision | `6e4ed1a2a79af7d8630a6b768ec859cb5af4d3be` |
| Upstream last modified | `2025-12-15T11:01:40.000Z` |
| Payload bytes | `446,564` |
| Payload SHA-256 | `35dc41080a3680858b27fa7e0533d2d547825316fc5dafe5d316f4ccc5a06132` |
| Rows / unique ids | `500 / 500` |
| Subject groups | `7` |
| Difficulty levels | `1, 2, 3, 4, 5` |
| Prompt-pack digest | `88c9d73588d19e87eef44565e7e7b490461c669fbe04b127227b501876f65dee` |
| Sample-manifest digest | `67cb3e35cde54e6a0a934196faf9398b3ae54393bfd87983ef2880afcb9bbfe0` |

The exact source is the immutable revision URL recorded in the JSON evidence. Runtime storage remains outside Git because the official data payload is recoverable from the pinned source and validated by the receipt.

## Fifteen Slices

1. Official source allowlist.
2. Official metadata response.
3. Full immutable commit revision.
4. Revision-pinned `test.jsonl` path.
5. Bounded and complete transfer.
6. Content SHA-256.
7. Exact 500-row count.
8. Required row schema.
9. Unique sample identifiers.
10. Official test-split contract.
11. Seven-subject coverage.
12. Five-level difficulty coverage.
13. Deterministic prompt-pack and 32-sample manifest digests.
14. Explicit evaluator disclosure.
15. Durable snapshot read-back and digest reverify.

## Product Integration

- `GET/POST /api/benchmarks/qualification` owns qualification and reverify actions.
- Benchmark Studio shows the pinned revision, integrity, check count, and official-score `HOLD` boundary.
- A passing receipt exposes `math-500-qualified` in the dataset selector.
- The server run planner reads the verified snapshot from durable storage.
- Runs preserve the official question and reference answer, but use `manual-review-compatibility` until the official mathematical-equivalence evaluator is integrated.
- `/experiments` exposes the same 15 checks through `GET/POST /api/experiments/v163-benchmark-qualification`.

## Real Runtime Smoke

After qualification, the product executed the first official test item through the normal feature-owned Benchmark route:

| Field | Value |
| --- | --- |
| Run id | `34aff595-3d79-423c-9b36-6d95116ef8fd` |
| Target | `local-qwen3-0.6b` / Local MLX Gateway |
| Workload | `math-500-qualified` |
| Item | `test/precalculus/807.json` |
| Execution | `1/1` successful samples |
| First-token / total latency | `1,686 ms / 1,688 ms` |
| Throughput | `94.79 tokens/s` |
| Scored samples | `0` |

The run confirms snapshot-to-planner-to-local-runtime integration. It deliberately records `score: null` and `passed: null`; the generated answer is compatibility evidence only and was not promoted as an official MATH-500 result.

## Verification

- Real upstream qualification: `15/15 PASS`.
- Snapshot read-back: `verified`.
- Full repository tests: `43/43 PASS`, including qualification failure cases and v1.6.3 receipt projection.
- Changed-file TypeScript gate: all `11/11` affected partitions passed.
- Cross-surface CI route smoke: `38/38 PASS`.
- Full release route smoke: `PASS` after updating three stale `v1.3.1` assertions to the active `v1.5.1` truth and raising the dense-suite request ceiling from 20 to 30 seconds; the initially timed-out Local Server evidence endpoint then returned normally.
- Architecture budgets: `6/6` targets met with zero debt lines.
- Durable-state boundary validation: `PASS`; the qualification service is classified as durable.
- Source hygiene: `795` files passed.
- Production dependency audit: `0` known vulnerabilities.
- Release truth: `15/15 PASS`; source remains `1.5.1`, distribution `HOLD`, production `BLOCKED`.
- Machine-readable evidence: [`v1.6.3-benchmark-qualification-latest.json`](./v1.6.3-benchmark-qualification-latest.json).

## Remaining Gate

The next benchmark quality slice is the official MATH-500 answer normalization and equivalence evaluator, followed by a pinned full-dataset model run and reproducibility comparison. MMMU, MathVista, MMBench, Video-MME v2, and paid native MiniMax M3 output-quality evidence remain open. This local dataset receipt does not alter external identity, distributed runtime, cloud signing/archive, Apple distribution, or organization acceptance gates.
