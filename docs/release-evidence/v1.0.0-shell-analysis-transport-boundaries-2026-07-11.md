# v1.0.0 Shell, Analysis, and Transport Boundary Evidence

Date: 2026-07-11

## Scope

This continuation completed 15 non-cloud GA boundary slices:

1. Removed the zero-caller `AgentCompareLab` compatibility export.
2. Removed `AgentRecipeGallery` and the old `components/agent/compare/*` compatibility barrel.
3. Physically moved Get Code into `features/agent`.
4. Extracted Agent conversation selectors.
5. Extracted Agent session-sync status projection.
6. Extracted the Agent workbench header.
7. Extracted the Agent runtime/status identity band.
8. Extracted the Agent starter/Compare prompt strip.
9. Added feature-owned session-tools props assembly.
10. Added feature-owned transcript props assembly.
11. Added feature-owned composer props assembly.
12. Added feature-owned secondary-analysis and Get Code props assembly.
13. Moved Admin telemetry charts and number formatting into `features/admin`.
14. Moved Admin runtime formatters and Benchmark heatmap/failure analysis into their owning features.
15. Removed `NextResponse` from the runtime application layer and kept HTTP serialization in the route.

## Verification

- `npm run typecheck:changed`: PASS, 11/11 partitions.
- Focused `agent-ui`, `agent-api`, and `admin` typechecks: PASS.
- Final route smoke: PASS, 41/41 checks.
- UI routes: 9/9.
- API contracts: 28/28.
- Compatibility headers: 4/4.
- Route-smoke integrity: SHA-256 verified.
- Route-smoke history after this batch: 11 entries.
- Consecutive full passes after the cold-start event: 3.
- Release security preflight: PASS, 550 candidate files, zero secret findings, zero production dependency vulnerabilities.
- GA bundle: SHA-256 integrity verified and live state `in-sync`.
- v1.0 release matrix: 85%, `evidence-needed`.

## Cold-start Evidence

The first cold-start smoke recorded one real failure: `/api/finetune` completed in 20.007 seconds, just after the smoke client's 20-second timeout. The server returned HTTP 200 and logged no runtime exception. After compilation, a direct request completed in 52 ms; three subsequent full smoke runs passed. The failed archive remains in history and was not deleted or rewritten.

## Gate Truth

This local refactor does not satisfy remote Provider freshness, the 2026-09-30 compatibility sunset, AWS workload identity, KMS, Object Lock, or tied failover evidence. No paid remote request or cloud production rehearsal was run for this batch.
