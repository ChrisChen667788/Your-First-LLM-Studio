# v1.1.1 Model Hub Real Transfer And Physical Storage Evidence

Date: 2026-07-16

## English

### Real workload

- Provider: ModelScope.
- Repository: `onnx-community/tiny-gpt2-ONNX`.
- Requested revision: `master`.
- Resolved immutable revision: `7ee11395b1add2f56e7dd77f08113760b1fb99d8`.
- Selection: 9 configuration, tokenizer, vocabulary, merge, and ONNX files.
- Downloaded and SHA-256 verified: `9/9`, `8,288,366` bytes.
- Stable combined evidence digest: `63abfeac2aa480f671d73e41d790486dd51d9e0653b692173ece9b577892d202`.
- Internal source: the durable Model Hub download registry under the local application data directory.
- Physical destination: `HP ZHAN SSD`, ExFAT over USB, under `FirstLLMStudio/models-v111-evidence`.
- Source policy: preserved after verification. No destructive approval was supplied.

The worker resolved the public ModelScope repository API to one immutable commit, downloaded every selected file, compared every computed SHA-256 with the provider manifest, copied the directory through same-volume staging, re-hashed every external file, atomically renamed the destination, removed ExFAT AppleDouble metadata, and wrote a volume-bound ownership manifest.

### Gate result

The transfer receipt and physical migration receipt both pass. The combined promotion gate is `HOLD` with exactly one blocker: `authenticatedHubReceipt`.

The previously supplied ModelScope token returned HTTP `401` from the official `GET /openapi/v1/users/me` identity endpoint. The token was injected without terminal echo, was not written to `.env.local`, receipts, logs, or this repository, and is not treated as valid authentication evidence. Run the same workflow with a refreshed token and without `--allow-anonymous` to close the final gate.

```bash
MODELSCOPE_API_TOKEN='<fresh token>' npm run dev
npm run evidence:v111-model-hub -- \
  --run-id <timestamp> \
  --external-root '/Volumes/HP ZHAN SSD/FirstLLMStudio/models-v111-evidence' \
  --publish docs/release-evidence/v1.1.1-model-hub-promotion-2026-07-16.json
```

Machine-readable receipt: [`v1.1.1-model-hub-promotion-2026-07-16.json`](./v1.1.1-model-hub-promotion-2026-07-16.json).

High-resolution product evidence: [`models-studio.png`](../assets/screenshots/models-studio.png), captured at `3840x2400` with the same `7/8` gate state.

CI maintenance in the same slice upgrades `actions/checkout`, `actions/setup-node`, and `actions/upload-artifact` to `v7` while the project test runtime remains Node.js 22. The pushed GitHub Actions run is the authority for confirming that the former Node 20 action-runtime deprecation annotation is gone.

Official references: [ModelScope OpenAPI bearer authentication](https://modelscope.cn/docs/openapi) and [ModelScope multi-file download](https://modelscope.cn/docs/models/download).

## 中文

本轮已完成真实公开仓库多文件下载与物理外置盘迁移：ModelScope manifest 被解析到不可变 commit，9 个文件全部下载并通过官方 SHA-256 对照，共 `8,288,366` 字节；随后通过同卷 staging、逐文件复核和原子 rename 迁移到 USB 外置 `HP ZHAN SSD`，并写入绑定 volume UUID 的 ownership manifest。源目录默认保留，没有执行删除。

当前 transfer receipt 和 physical migration receipt 均为 PASS，组合 promotion gate 为 `7/8 PASS`。唯一 HOLD 是 `authenticatedHubReceipt`：旧 ModelScope token 在官方 `users/me` 接口返回 `401`。系统没有保存或输出 token，也没有把匿名公开下载表述为认证通过。更新 token 后按上面的命令重跑即可完成最后门禁。

同一批次已将 GitHub Actions 的 `checkout`、`setup-node`、`upload-artifact` 升级到 `v7`，项目测试运行时继续固定 Node.js 22。旧 Node 20 action-runtime 弃用提示是否消失，以本次提交推送后的真实 Actions run 为准。
