# v1.0.0 Workspace and Runtime Card Boundary Evidence

Date: 2026-07-11

## Ten Development Slices

1. Extracted Agent sidebar content composition.
2. Added target-catalog and target-profile props assembly.
3. Extracted the Agent main workspace composition.
4. Added header, status-band, and prompt-strip props assembly.
5. Added a feature-owned Chat/Compare mode render port.
6. Extracted the Admin per-target runtime view-model.
7. Extracted the Admin hardware metrics grid.
8. Extracted the Admin runtime log search and summary panel.
9. Extracted the remote runtime status builder from local gateway orchestration.
10. Updated ownership contracts and completed release verification.

## Resulting Boundaries

- `AgentWorkbench.tsx` creates state/actions and returns one `AgentWorkbenchLayout` input.
- Sidebar, main workspace, Chat/Compare switching, and Get Code placement live under `features/agent`.
- `AdminDashboard.tsx` no longer filters six runtime metric histories or renders log search/summary controls inline.
- Remote runtime credential, phase, model, and resource payload construction is separate from local gateway health handling.

## Verification

- `npm run typecheck:changed`: PASS, 11/11 partitions.
- Focused `agent-ui`, `admin`, and `agent-api` typechecks: PASS.
- Cold-start route smoke: PASS on the first run, 41/41 checks.
- UI routes: 9/9.
- API contracts: 28/28.
- Compatibility headers: 4/4.
- Route-smoke SHA-256 integrity: verified.
- Route-smoke history: 12 entries, 4 consecutive full passes.
- Release security preflight: PASS, 559 candidate files, zero secret findings, zero production dependency vulnerabilities.
- Final GA bundle: SHA-256 verified, `in-sync`; v1.0 remains 85% `evidence-needed` with six external blockers.

## Gate Truth

The new smoke archive temporarily made the persisted GA bundle drift; it was then rewritten from current sources and verified `in-sync`. Remote Provider freshness, the 2026-09-30 compatibility sunset, AWS workload identity, KMS, Object Lock, and tied failover evidence remain external blockers. No remote paid request or cloud production rehearsal was run in this batch.
