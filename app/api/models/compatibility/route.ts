import { NextResponse } from "next/server";
import { evaluateModelCompatibility, readModelCompatibilityEvidence, type ModelCompatibilityInput } from "@/features/models/compatibility-manifest";
export const runtime = "nodejs"; export const dynamic = "force-dynamic";
export async function GET() { return NextResponse.json(readModelCompatibilityEvidence()); }
export async function POST(request: Request) { try { const body = await request.json().catch(() => ({})) as Partial<ModelCompatibilityInput>; if (!body.modelId || !body.format || !body.runtime) throw new Error("modelId, format, and runtime are required."); const receipt = evaluateModelCompatibility({ modelId: body.modelId, format: body.format, runtime: body.runtime, license: body.license || "", tokenizerFiles: body.tokenizerFiles || [], chatTemplate: body.chatTemplate, parameterBillions: body.parameterBillions || 0, quantizationBits: body.quantizationBits || 16 }); return NextResponse.json({ ok: receipt.status === "pass", receipt, evidence: readModelCompatibilityEvidence() }, { status: receipt.status === "pass" ? 200 : 422 }); } catch (error) { return NextResponse.json({ ok: false, error: error instanceof Error ? error.message : "Compatibility evaluation failed." }, { status: 400 }); } }
